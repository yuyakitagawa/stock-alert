# 大口投資家の監視ブログ

EDINET大量保有報告書などの公開情報をもとに、機関投資家・インサイダー・自社株買いなど
「クジラ」（相場を動かすほどの資金力を持つ大口投資家の俗称）の動きを監視・解説するブログ。
ブランド名は「大口投資家の監視ブログ」、ドメインは`kujira-watch.com`（クジラのイメージで確保）。
SEO/AIO（AI Overview・LLM引用）対策済み。

デプロイ先: https://kujira-watch.com/ （旧URL: https://stock-alert-lyart.vercel.app/ 。
進捗はリポジトリルートの `docs/progress_blog_seo_aio.md` を参照）

## スタック

- Next.js 16 (App Router) + TypeScript
- MUI (Material UI) v9 + Emotion（`@mui/material-nextjs`でApp Router用SSR配線。テーマは`src/theme.ts`、ブランドカラー[紺/青/金]を反映）。**「開くまで表示されない」重いコンポーネントは`next/dynamic`で遅延読み込みする**（`StockSearchPanel`のAutocomplete/TextField、`HeaderMenuDrawer`のDrawer=Modal/Portal/Backdrop/Slide一式）。どちらも閉じているのが既定なのに全ページの初期JSに積まれていた。見た目・挙動は据え置きで読み込みのタイミングだけ後ろにずらす方式なので、Material Designの構成は変えずに初期JSを約33KB(gzip)削れる
- Tailwind CSS v4（ページレイアウト・グリッド・`@tailwindcss/typography`でのリッチテキスト本文装飾を担当。コンポーネント単位のスタイルはMUI側）。一覧のカードUIは`src/app/globals.css`の`@layer components`に`.card`/`.card-grid`/`.card-grid-wide`として定義する（索引ページは1ページに100件以上並ぶため、ユーティリティクラスの羅列やMUI Cardではclass属性だけでHTMLが肥大化する。`@layer components`に入れるのは、レイヤー無しで書くとTailwindのユーティリティより優先されて`flex`等での上書きが効かなくなるため）
- microCMS（`microcms-js-sdk`）
- Supabase（`@supabase/supabase-js`。フッターの累計訪問者カウンター用。トレーディングシステム側と同じプロジェクトの`blog_visit_counter`テーブル+`increment_blog_visit_counter` RPC。加えて`/stocks/[code]`の会社情報カードが同プロジェクトの`jpx_stock_list`・`gen_rankings`テーブルを、`/investors`・`/investors/[filer]`が`edinet_large_holdings`・`edinet_filer_classification`・集計ビュー`edinet_filer_summary`を参照）
- Vercel想定（ISR: `revalidate = 60`、`@vercel/speed-insights`でCore Web Vitals計測。アクセス計測はGA4のみ＝`@vercel/analytics`はProプランだと無料枠なしの従量課金になるため2026-09-03に撤去）。CDN（Vercel Edge Network）のキャッシュはページのISRに加え、APIルート（`/api/articles`・`/api/stocks/search`に`Cache-Control: s-maxage + stale-while-revalidate`）・`/feed.xml`（`revalidate = 300`でISR化）・画像最適化（`images.minimumCacheTTL` 31日）でも明示的に効かせている。 ビルドの要否は`vercel.json`の`ignoreCommand`（Ignored Build Step）で判定し、main以外のブランチとkujira-watch/に差分の無いコミット（docsのみ・keepalive）はビルドしない（2026-09-03。Vercel側はビルドマシンStandard固定＋オンデマンド並列ビルドOFF＝ビルド課金なし）。設計の考え方と動作確認方法は `docs/cdn_study.md` を参照

## セットアップ

```bash
npm install
cp .env.local.example .env.local
```

`.env.local` に microCMS サービスの値と、SEO用のサイトURL/サイト名を設定する。

```
MICROCMS_SERVICE_DOMAIN=xxxx
MICROCMS_API_KEY=xxxx
NEXT_PUBLIC_SITE_URL=https://kujira-watch.com
NEXT_PUBLIC_SITE_NAME=大口投資家の監視ブログ
NEXT_PUBLIC_LINE_ADD_FRIEND_URL=  # 読者向けLINE公式アカウントの友だち追加URL（未開設のため未設定。設定するとFollowCtaにLINEボタンが出る）
```

`NEXT_PUBLIC_SITE_URL` / `NEXT_PUBLIC_SITE_NAME` は独自ドメイン・ブランド名が決まった際に
値を差し替えるだけで、metadata・OGP・構造化データ・サイトマップの全ページに反映される
（未設定時は現行のVercelドメイン・現行ブランド名にフォールバックする）。

```bash
npm run dev
```

## microCMS側の前提（APIスキーマ: `articles`）

管理画面のGUIで以下のフィールドを持つ `articles` エンドポイント（リスト形式）を作成しておくこと。

| フィールドID | 表示名 | 型 | 必須 |
|---|---|---|---|
| title | タイトル | テキスト | ○ |
| body | 本文 | リッチエディタ | ○ |
| stockName | 銘柄名 | テキスト | ○ |
| stockCode | 証券コード | テキスト | ○ |
| dealType | 投資家分類 | セレクトフィールド（個人／創業家の資産管理会社／公益/一般財団法人／プライムブローカー／アクティビスト／VC／PE・メザニンファンド／独立系ブティックAM／国内アセットマネジメント／外資系伝統運用会社／日系証券銀行／事業会社／その他） | ○ |
| dealDate | 取引日 | 日付 | ○ |
| dealAmount | 金額規模（億円） | 数値 | ○ |
| sourceUrl | 出典URL | テキスト | △ |
| tags | タグ | テキスト（カンマ区切り。売り方向の記事には`"売り"`を含める） | △ |
| eyecatch | アイキャッチ画像 | 画像 | △ |
| filerName | 取引企業（提出者名） | テキスト | △（2026-08-15にスキーマ作成済み。下記の注意書きを参照） |
| ratioChangePct | 保有比率の変化幅（ポイント、売りは負値） | 数値 | △（2026-08-15追加。記事詳細のファクトボックス「前回比」に使用） |

> **注意（2026-08-15更新）**: `filerName` は長らくmicroCMSスキーマに存在せず（microCMSは
> スキーマに無いフィールドを黙って捨てるため、`web/publish_blog_articles.py`が送っても
> 全記事で値が空だった）、フロント側は提出者名をEDINET開示（Supabase `edinet_large_holdings`）と
> 「銘柄コード×取引日」で突き合わせて補ってきた（`src/lib/investors.ts` の
> `getFilerNamesByStockAndDate()`。同じ銘柄・同じ日に複数の提出者がいる開示＝2026年8月実測で
> 全体の約7%は、誤った帰属を避けるため除外する）。2026-08-15に`filerName`・`ratioChangePct`の
> 両フィールドが管理画面で作成されたため、**以降の新規記事はCMSの値がそのまま入る**
> （フロント側はCMSの値を優先し、値が空の既存記事のみ突合フォールバックを使う実装）。


## ページ構成

| パス | 内容 |
|---|---|
| `/` | 記事一覧（EDINET開示のうち解説記事になったものの一覧。かつては同じ開示を全件並べる`/disclosures`（開示速報）を別に持っていたが、2026-08-18時点の実測で1日40件の開示に対し記事25件がその完全な部分集合＝TOPと同じイベントを二重に見せているだけで、EDINET取込と記事投稿は`edinet_blog.yml`の同一実行の連続ステップなので速報性の差も無く、sitemapに載るのも1URLだけだったため2026-08-18に`/disclosures`を廃止した。失われる一次ソースへの導線＝EDINET原文PDFリンクは`/stocks/[code]`・`/investors/[filer]`の開示履歴表へ移設済み）。H1「大量保有報告書で読む大口投資家の動き（{開示日}の取引）」（2026-08-23に「{開示日}の取引」から変更。日付だけのH1では主要検索語が入らず、初見の訪問者に何のサイトか伝わらないため、主要語を含む見出しと1文の自己紹介リードを置く。2026-08-18には「今日の注目取引」→「注目の取引」と改称していた。「今日」は日付をまたぐと実態とずれるため）の直下に「{開示日}の大口取引」サマリー（2026-08-18に「今日のクジラ」→「今日の大口取引」→日付主語へ改称。日付をまたいで閲覧しても実態とずれないようにするため）（`src/components/TodayWhaleSummary.tsx`。最新の取引日・その日の買い/売りの推定金額を大きく出し、`/date/[date]`へリンクする。**開示件数は出さない**（2026-09-03、オーナー指示。数えられるのは記事になった開示だけで、足切りや日次上限で記事にしなかった開示は数に入らないため「N件の開示」と出すとその日の開示自体が少なかったように読める）。毎日更新されていることが一目で分かるようにするための鮮度表示で、金額は初回取得30件で切れないよう`getArticlesByDealDate()`で取り直す。EDINETの受付終了後（JST18時以降）と過去日は「この日の開示は確定」、当日の受付中は「受付中」と出す＝`src/lib/jst.ts`の`areDisclosuresFixed()`、2026-08-19追加）を置き、続けて銘柄ランキングの上位3件（`src/components/TopTrendingPreview.tsx`。/trendingと同じ集計＝直近7日 vs その前の7日で開示が増えた銘柄を推定売買金額順に並べた先頭3件を、銘柄名・件数・金額と`/stocks/[code]`リンクで出し、`/trending`への導線を添える。2026-08-27追加: GA4のクリックログ実測でTOPは閲覧者102人中18人＝17.6%しか何も押しておらず全ページ中最低なのに、/trendingは閲覧者8人全員・/rankingは83.3%が押していた。ヘッダーのタブに同じリンクは並んでいるので、足りないのはリンクではなく押す理由＝実際の銘柄名と金額だと判断した。Supabaseが落ちても記事一覧は出るよう取得は握りつぶし、0件ならブロックごと出さない）を出し、続けて「{開示日}の買い開示は、その後どうなったか」枠（`src/components/TopReturnPreview.tsx`＋`getLatestReturnCohort()`。直近で3ヶ月の判定期間を満了した開示日ぶんを`investor_return_positions_3m`から取り、その日の全件の平均リターンと勝率を先に書いたうえで、内訳の上位3件を銘柄名・提出者名・3ヶ月後リターンで出す。上位だけを並べると勝った銘柄しか出ない見せ方になるため、母数の平均と勝率の併記を必ずセットにする。2026-08-30追加）を出し、続けてカテゴリ絞り込み（`src/components/CategoryFilterDetails.tsx`、MUI Accordionで開閉、`/category/[category]`へのボタン）を配置し、その下に「注目」枠として金額規模上位3件をヒーロー枠で表示（`getFeaturedArticles()`＝`lib/microcms.ts`。直近20件のプールを取引日の新しい方から3開示日ぶんに絞り、その中で推定取引金額が大きい順に3件。日付で絞らないと古い大型取引が居座る＝2026-09-02時点で8/31・8/28・8/28の3件が並び最古は5日前だったため、`FEATURED_DAY_WINDOW=3`を追加。絞った結果が3件未満のときだけプール全体に戻す。同じロジックを`web/publish_blog_articles.py`の`get_featured_article_ids()`が持つので変更時は両方直すこと）し、続けてXフォロー導線（`FollowCta`、記事ページと同じバナー。2026-08-23追加、サイトの主要コンバージョンであるXフォローの導線が記事末尾にしか無かったため）を置く。初回30件をサーバー側でレンダリングし、下端までスクロールすると自動で次の10件を読み込むオートスクロール方式 |
| `/weekly` | 大口投資家の週次トレンド（2026-08-18に「動きまとめ」から週次トレンド化。直近7日間の横断要約に加えて週ごとの推移を出す。「大口投資家の動きを教えて」等の包括的な検索・LLMクエリに直答するための集約ページ。冒頭に直近7日間の開示件数・推定取引金額（前週比つき）のタイルを出していたが、取得と売却を合算した規模で方向が読めずグラフと比べて伝える情報が無いため2026-08-22に削除し（専用だった`getPreviousPeriodArticles()`・前週比の算出も削除）、グラフを主役にした。「週別の売買金額トレンド」（`getRecentArticleDigests()`＝記事のdealDate/dealAmount/tags/dealTypeのみ8週分取得し、週ごとに買い・売りへ分けて`AmountTrendChart`（買いを上・売りを下へ伸ばす上下対称の棒グラフ。インラインSVG自前描画、2系列なので凡例あり、今週は薄色で「集計中」、数値ラベルは全週に表示（当初は最新週のみだったが2026-08-22に全週へ）、各棒の`<title>`でホバー時に買い/売り/差し引き/件数）で表示し、件数・買い・売り・差し引きの全数値は`<details>`内の表で参照できる＝2026-08-21にグラフ化。開示が0件の週も横軸の枠として残す（週が飛ぶと「その週は少なかった」と誤読されるため。記事データがまだ無い最古側の空週だけ落とす））と「投資家分類別の週次トレンド」（`CategoryTrendGrid`＝投資家分類1つにつき`AmountTrendChart`と同じ「買いは上・売りは下」の小さな上下対称棒グラフを1枚ずつ描き、グリッド（sm:2列／lg:3列）に並べる。同じ分類の買いと売りが隣にあることで「この分類は今買っているのか売っているのか」が1枚で読めるようにしている。縦軸スケールは分類ごと（カード右上に「目盛り ◯億」＝その分類の最大値を表示。当初は全分類共通にしたが最大の分類に引っ張られて他の棒が数px以下になり見えなかったため2026-08-22に分類ごとへ変更）＝分類間の規模比較はカード右上の期間計で行う。各カードの右上に期間計の買い越し/売り越し額、直接ラベルは全週に表示（2026-08-22に最新週のみから全週へ）、各棒の`<title>`で買い/売りの金額と件数、全数値は`<details>`内の表で参照できる。売買金額トレンドと同じ週区切りの直近6週から記事データが無い最古側の列を落として表示し、並びは期間計（買い＋売り）の降順。最新週の買い・売り上位3分類を文章でも出していたが、グラフと重複し冗長なため2026-08-22に削除。2026-08-22に「買い用・売り用の2枚のヒートマップ表」として追加したが、同じ分類の買いと売りが離れて読みにくいため同日中に分類ごとのグラフへ作り直した）の2ブロックだけを並べる。「集計中」（薄色表示）の判定は「今日（JST）が今週の月〜金」のときだけ＝EDINET開示も記事生成（平日9〜21時JST）も平日にしか動かないため、土・日は今週分を確定扱いにする（暦週だけで判定すると日曜まで集計中と出てしまう＝2026-08-22修正）。「週別の開示件数トレンド」（EDINET全開示の件数）・「直近7日間のポイント」（買い/売りカード）・「注目の取引」（金額上位3件のカード）・「日別の記事一覧」（`/date/[date]`へのリンク、`DailyDigestList`ごと削除）は売買金額トレンドや分類別トレンドと内容が重複する／個別記事は月別アーカイブから辿れるため2026-08-22に削除し、ページを「今どの投資家がどちらへ動いているか」だけに絞った。これにより/weeklyから`/date/[date]`への内部リンクは無くなり、取引日別ページのクロール経路は「ヘッダー→月別アーカイブ→各月→各日」（`/monthly`ハブ）に一本化された。ItemList構造化データも本文でリンクする対象が無くなったため廃止し、BreadcrumbListのみ残している。全記事をカード表示していた旧構成も縦に長すぎるため廃止。末尾に「直近の大型取引の解説記事」＝アイキャッチ付き記事カード4件（最新20件から金額上位・1銘柄1件、`RelatedArticles`。2026-08-22の全タブ画像展開）。分類別トレンドの各カード見出しには色ドットに代えて分類アイコン（`DealTypeIcon`＝自作SVGラインアイコン＋分類色背景、2026-08-23）。ヘッダーから常時リンク）。**冒頭の直答文＋ファクトボックス（2026-08-29）**: h1直下に「直近7日間（M/D〜M/D）に大口投資家の開示はN件で、買いX億円・売りY億円、差し引き◯◯越しZ億円でした。買いの金額が最も大きい投資家分類は「◯◯」です」という数字入りの1文と、開示件数・買い・売り・差し引きの`FactBox`を置く（`buildRecentSummary()`）。AI検索は見出し直下の短い断定文と定義リストの数値を抜くため（GEO）。2026-08-22に削除した「取得と売却を合算した規模」のタイルとは違い、買いと売りを分けて方向が読める形にしてある。 |
| `/activists` | アクティビスト注目銘柄（2026-08-18に「保有銘柄一覧」から改名・再構成、2026-08-20に「直近のアクティビストの動き」セクションを削除し注目銘柄のみの1本構成へ）。直近30日にアクティビストが保有比率を増やした＝買い入れた銘柄を、増加幅(pt)の銘柄別合算が大きい順に全件を一度に表示（2026-08-21に初期10件＋「もっと見る」をやめ、`ShowMoreList`ごと削除）。前回比率が無い開示は新規保有として今回比率ぶんを増加扱い。2ファンド以上が同時5%以上保有中の銘柄には「複数ファンド保有」バッジ。定義の解説は`/faq/usage`のQ&Aへ。一覧は`.card-grid-wide`の1グリッド。HTMLに載せる件数には上限＝100銘柄を設けており（2026-08-21に30→100へ拡大。直近30日の買い入れ銘柄は50件台で、実質全件が載る）、打ち切った場合はその件数を本文にも明記する。集計元は`getActivistRecentMoves(30)`（直近30日の開示）、複数ファンド保有バッジの判定は`getActivistHoldingsSummary()`（提出者名を15件ずつ`.in()`分割で問い合わせ、(提出者×銘柄)の最新行を採用）。どちらも`src/lib/activists.ts`。銘柄は上場銘柄マスターに載っているコードをリンク（`getAllListedCodes()`。`/trending`と同じ規律）。提出者名は表示のみ`displayFilerName()`で半角へ寄せ、hrefは原文のまま。`unstable_cache`で1時間キャッシュ、ヘッダーから常時リンク。一覧の下に「アクティビストの解説記事」＝アクティビスト分類の最新記事8件のアイキャッチ付きカード（`RelatedArticles`、2026-08-22。件数は2026-08-29に4→8）。各銘柄カードに業種アイコン（`SectorIcon`＝JPX33業種の自作SVGラインアイコン、会社ロゴは商標・取得元の問題で使えないため業種で代替。sectorは`getCompanyBriefs()`で表示分を一括取得、2026-08-23）。2026-08-29に4ページ共通の視覚強化を実施（データ4ページは「見出し→細いグレーの説明文→小さいカードの羅列」でファーストビューに視線が止まる要素が無く、画像が最下部の記事カードにしか無かったため）: ①各カードの数値に量感バー（`MagnitudeBar`。値/表示分の最大値で幅を出す横バー。1件あたりのHTMLを増やさないようSVGではなくspan2つ＋inline widthで描く。1位のみ金色）②業種アイコンを16px線画→40pxの角丸カラーチップへ（`SectorIcon` `size="lg"`。33業種を資源・素材＝紺／ものづくり＝青／生活・商業・サービス＝金／金融・インフラ・運輸＝濃紺の4グループ色に割り当て。新色は足していない）③記事アイキャッチを「先頭1件が3列中2列ぶんの幅＋最大8件」へ（`RelatedArticles`）（このページのバーは買い入れ幅pt）。ヘッダー上タブのラベルは長くて他タブを押し出すため「アクティビスト」に短縮（`lib/nav.ts`、ページタイトルは据え置き、2026-08-23）。**冒頭の直答文＋ファクトボックス（2026-08-29）**: h1直下に「アクティビストは直近7日間にN銘柄を買い入れました。買い入れ幅が最も大きいのは◯◯（コード）のXptです。現在アクティビストが大量保有を開示している銘柄はM銘柄で、うちK銘柄は複数ファンドが同時に保有しています」という1文と、買い入れ銘柄数・最大の買い入れ幅・保有中の銘柄数・複数ファンド保有の`FactBox`を置く（GEO対策）。数値はページ下部の一覧と同じ集計。 |
| `/buybacks` | 自社株買い注目銘柄（2026-08-21新設）。TDnet開示の原文PDFから抽出した取得枠（`lib/buybacks.ts`←Supabase `tdnet_buybacks`）をもとに、上限金額ランキング（15件、`RankingList`。各カードに発行済比率を補足表示、業種アイコン`SectorIcon`。当初は発行済比率／上限金額の2ランキング並列だったが「比率順位の意味が伝わらない」ため2026-08-23に金額順1本へ統合）→最新の決定一覧（開示日降順・最大100件。当初はMUI Table（minWidth 720px）でスマホは横スクロールが必要だったため、2026-08-23に他の一覧と同じ1件1カード（`.card-grid-wide`＋業種アイコン）へ変更）→「自社株買いの解説記事」（`RelatedArticles`8件。2026-08-29に他のデータページと同じ帯へ統一。それまでは`ArticleCard`6件の2列グリッド）（「月別の決定件数」表は最新一覧・ランキングと比べて意思決定に使える情報が無かったため2026-08-23に削除。集計関数`getMonthlyBuybackCounts()`ごと削除）→FAQ（アコーディオン＋FAQPage JSON-LD。冒頭にあったサマリータイル（決定件数・上限合計・最大枠・消却同時決議）はランキングと重複し意思決定に寄与しないため2026-08-23に削除。本文にあった「数字の見方」読み物セクションは2026-08-23にQ&A形式へ移設・統合）の構成。2026-08-29に4ページ共通の視覚強化を実施（データ4ページは「見出し→細いグレーの説明文→小さいカードの羅列」でファーストビューに視線が止まる要素が無く、画像が最下部の記事カードにしか無かったため）: ①各カードの数値に量感バー（`MagnitudeBar`。値/表示分の最大値で幅を出す横バー。1件あたりのHTMLを増やさないようSVGではなくspan2つ＋inline widthで描く。1位のみ金色）②業種アイコンを16px線画→40pxの角丸カラーチップへ（`SectorIcon` `size="lg"`。33業種を資源・素材＝紺／ものづくり＝青／生活・商業・サービス＝金／金融・インフラ・運輸＝濃紺の4グループ色に割り当て。新色は足していない）③記事アイキャッチを「先頭1件が3列中2列ぶんの幅＋最大8件」へ（`RelatedArticles`）（このページのバーは発行済株式比率）。`revalidate=3600`、ヘッダーから常時リンク |
| `/articles/[id]` | 記事詳細。銘柄｜取引日｜金額規模｜保有比率（提出者が特定できた記事のみ`getHoldingSnapshot()`でEDINET開示から取得、直前の保有割合があれば「（前回 X%）」を併記）｜前回比（**EDINET開示の「今回比率−直前保有割合」を正とし、取れないときだけCMSの`ratioChangePct`にフォールバック**。CMS側の値は記事生成時にXBRLの直前保有割合が取れていないと今回比率の全量が入ってしまい、「保有比率10.57%（前回10.72%）」と「前回比−10.57pt」が同じ画面に並ぶ矛盾が起きていた＝2026-08-19に是正。±pt表示で買いは緑・売りは赤）｜取引企業（`/investors/[filer]`への内部リンク。CMSの`filerName`が空の旧記事はEDINET開示との突合で解決する。上記スキーマの注意書きを参照。突合できない場合は列自体を出さない）｜**保有目的**（開示原文の記載をそのまま出し、`classifyPurpose()`が寄せた5区分「重要提案行為等／経営参加／政策保有／安定株主／純投資」のバッジ＝`src/components/HoldingPurposeBadge.tsx`を頭に付ける。同じ「5%取得」でも純投資と重要提案行為等では読み手にとっての意味が違うため。分類は自由記述からの機械判定なので原文を必ず併記する）｜**重要提案行為等**（この欄に具体的な記載がある開示だけ。大半は「該当事項なし」なので出さない）｜**平均取得単価（開示ベース）**（`averageAcquisitionPrice()`＝取得資金の総額÷保有株数。EDINETは通常「比率」しか出さないが取得資金は本表に金額で載っているため、概算でない取得原価が出せる。保有が古いほど現在株価から乖離するので参考値扱い）｜**借入比率**（`borrowingRatio()`＝借入金÷取得資金。自己資金0＝全額借入の買いが実在し、返済圧力がある分だけ同じ保有比率でも意味が違う。50%以上は警告色）｜**報告義務発生日**（提出まで30日を超えた開示だけ。法定は5営業日以内で、大幅に遅れた開示は「株価が動いた後に出てきた開示」として読む必要があるため）のdl（ファクトボックス）を表示し、直下に開示ラグ（報告義務発生から提出まで最大5営業日）と投資助言ではない旨の定型注記を置く。金額規模には「（概算）」を添える（EDINETは比率しか開示せず、金額は発行済株式数×株価×比率変化の概算のため）。ファクトボックスの下には「開示後の株価推移」（`src/components/PriceAfterDisclosure.tsx`＋`src/lib/priceReturns.ts`。Supabase `yahoo_price_cache`から開示日の基準終値・+1ヶ月(21営業日)・+3ヶ月(63営業日)・直近の騰落率を表示。まだ到来していない地点は空欄にせず「開示から21営業日後に表示」と出す。株価データが無い銘柄では非表示）と「この投資家の保有比率の推移」（`src/components/HoldingRatioChart.tsx`＋`getHoldingHistory()`。同一銘柄×同一提出者の開示を古い順に並べたインラインSVGの折れ線。2件以上あるときだけ描画。1回ぶんの前回比だけでは積み増し中か撤退中かが分からないという指摘への対応、2026-08-19追加）を置く。本文中の「※推測:」で始まる段落は`frameSpeculation()`が「編集部の見立て」枠（aside）に切り出し、事実と混ざらないようにする。記事末尾のタグは運用フラグの`自動生成`を除いて表示する。本文の下には出典ブロック（情報源=EDINET・提出日・記事公開日・元の開示リンク・免責の定型文。E-E-A-T/AIO対策）、共有ボタン（`src/components/ShareButtons.tsx`、X/LINEのWeb Intentへのリンク。SDKは読み込まず`ActionButton`の外部リンクとして描画。はてブは利用実績がなく2026-08-15に削除）、Xフォロー導線（`src/components/FollowCta.tsx`、ネイビー地の反転配色バナー。フォローintent `x.com/intent/follow` への素のリンク。`NEXT_PUBLIC_LINE_ADD_FRIEND_URL`設定時はLINE友だち追加ボタンを先頭に出す）と、回遊導線として「同じ銘柄の他の記事」（`getArticlesByStockCode()`、最大3件の一行リンク＋銘柄ページへの導線）「この取引をした投資家」（投資家ページへの導線）「関連ランキング」（3ヶ月リターンランキング/銘柄ランキングと当日の全開示へのnav）「関連記事（同じ分類）」（カード最大4件）を重複排除して並べる。AEO（回答エンジン最適化）用に、本文の前に質問型H2「この開示で何が起きた？」＋EDINETの数値だけで組んだ直答1文（日付・提出者・銘柄・前回比率→今回比率・方向。提出者と保有比率が取れた記事のみ）、記事末尾の投資家導線のH2を「{提出者}とはどんな投資家？」＋分類名と`DEAL_TYPE_DESCRIPTIONS`の1文に変更（2026-08-23。強調スニペット／AI検索は質問見出し直下の短い1文を抜き出すため） |
| `/articles` | 記事一覧（全記事の通覧ページ。カテゴリページと同じ初回30件サーバーレンダリング＋オートスクロール、BreadcrumbList・ItemListの構造化データつき）。記事は800本以上あるのに一覧が無く、読者はトップの新着かカテゴリ・銘柄・投資家の集約ページ経由でしか記事に辿り着けなかった（2026-08-27のAdSense再監査で `/articles` が404であることを指摘され新設）。フッターの「一覧・アーカイブ」とサイトマップから参照 |
| `/*`（404） | 存在しないURLの案内ページ（`src/app/(ja)/not-found.tsx`）。既定ではNext.jsの素の404（ヘッダー・フッター・内部リンクなし）が出ており、記事の削除や開示の取り下げでURLが消えたときに行き止まりになっていた。トップ・記事一覧・銘柄一覧・投資家一覧・FAQへの導線を出し、`robots`は`noindex, follow`。2026-08-27のAdSense再監査での指摘を受けて追加 |
| `/category/[category]` | カテゴリ別一覧（同じく初回30件サーバーレンダリング＋オートスクロール） |
| `/stocks` | 銘柄一覧。見出し直下に業種（セクター）別の絞り込み（Supabase `jpx_stock_list.sector`から集計、`?sector=`クエリでSSRフィルタ、各業種の件数を`(N件)`で表示）を配置し、記事のある銘柄を証券コード順（辞書的に引ける順番）にカードで列挙（`lib/microcms.ts`の`getAllStocksForIndex()`＋`lib/companyInfo.ts`の`getSectorsByCode()`）。銘柄は約600件あるため`?page=`で100件ずつのページ送り（クロール可能な素の前後リンク。各ページは自分自身をcanonicalにする）。カードは`.card-grid`（3列相当）で並べる。1列のままカード化すると縦の総量が行リストの約3倍になり索引として使えないため。各カードに業種アイコン（`SectorIcon`、2026-08-23）。一覧の下に「最新の解説記事」＝アイキャッチ付き記事カード4件（`RelatedArticles`、2026-08-22） |
| `/stocks/[code]` | 銘柄ページ。見出し（企業名・証券コード）の直下に事業内容（あれば）を地の文で表示し（会社四季報の【特色】欄と同程度の密度＝2〜3文90〜130字。社名と証券コードだけでは何の会社か分からないため2026-08-18に1文40字から拡充）、続けて直近90営業日の株価推移グラフ・業種・終値・PER/PBR・52週レンジ位置・株主優待有無の会社情報カードを置く。その下にこの銘柄へ大量保有報告書を提出したことがある投資家を1件ずつ改行して一覧表示（`/investors/[filer]`への内部リンク。各行に分類バッジと、その投資家の最新開示の保有比率「保有比率 X.XX%（開示日時点）」を添え、保有比率の高い順に並べる＝2026-08-22追加。`getFilersByStockCode()`が`edinet_large_holdings`を開示日降順で読み、投資家ごとに最初の行を最新開示として採る。訂正報告書などで比率が欠損している投資家は比率を出さず末尾に回す）、「保有比率の推移」テーブル（`getHoldingsByStockCode()`でEDINET開示を開示日降順に最大100件、投資家リンク・種別・`RatioTransition`による前回%→今回%と▲/▼増減表示・EDINET原文PDFへの直リンク（`https://disclosure2dl.edinet-fsa.go.jp/searchdocument/pdf/{doc_id}.pdf`。`/disclosures`廃止に伴い2026-08-18に移設）。SEO/AIO 30日計画P3、2026-08-15追加）、続いて「大量保有・自社株買い履歴」の見出しを置き、同一`stockCode`の記事を`-dealDate`順に一覧表示する（解説記事が1本も無い銘柄ではこの節ごと省略し、開示テーブル＋会社情報＋FAQのページになる。EDINET開示はあるが記事化されていない銘柄が大半（開示のある2,982銘柄に対し記事があるのは599銘柄）で、以前は記事0件で404にしていたため検索から消えていた＝2026-08-18に解消。解説記事も事業内容の説明も無い銘柄のページは2026-08-29から公開しない＝404にしている（`src/lib/publishedPages.ts`。以前は`noindex, follow`で残していた）。上場銘柄マスター（`jpx_stock_list`）にも記事にも無いコードも404。末尾に「更新を追う」導線（`src/components/FollowUpdatesCta.tsx`＝銘柄別RSSと公式Xへのリンク、2026-08-19追加）と「よくある質問」（`src/lib/stockFaq.ts`の`buildStockFaqItems()`がそのページのEDINETデータから自動生成する2〜3問。可視のAccordion＋FAQPage構造化データを同一ソースから出す）。titleとh1は「◯◯（コード）の大株主・株主構成」で揃える（2026-08-19変更。GSC実測で上位クエリ8件がすべて提出者名で、銘柄側のクエリは「newmo 株主」1件・掲載順位10.2しか取れていなかったため、実際に検索される「銘柄名 大株主」「銘柄名 株主構成」の語に寄せ、検索されない「の動き」を削って社名が長い銘柄でもキーワードが省略されない位置に置いた。h1はそれまで社名＋コードだけでページの主題を示していなかった。「大量保有報告書」はdescriptionと本文側に残す）。記事詳細の「銘柄」欄から内部リンクあり。会社概要の下に「この銘柄をウォッチ」ボタン（`WatchStockButton`、localStorage保存・登録不要）を置き、押すと`/watchlist`への導線が出る。**冒頭の直答文（2026-08-29、`formatStockHolderLead()`）**: h1「◯◯（コード）の大株主・株主構成」＝想定クエリに1文目で答えるため、事業内容より前に「大量保有報告書を提出している投資家はN者。保有比率が最も高いのは◯◯（分類）のX%（YYYY/MM/DDの開示時点）。次いで◯◯がY%」を置く。それまでは答えが一覧と表の中にしか無く、AI検索が抜ける形になっていなかった。保有比率は開示のたびに変わるため開示日を必ず添える。 |
| `/watchlist` | **ウォッチ銘柄**（2026-08-27追加、`noindex`）。銘柄ページの「この銘柄をウォッチ」で保存した銘柄に出た大量保有報告書を、**前回このページを開いたあとに出たものだけ「新着」バッジ付き**でまとめて表示する。GA4の実測（28日）でユーザー364人に対し`first_visit`が363件＝ほぼ全員が一度きりの訪問で、既存の再訪導線はRSS（対象単位）と公式X・YouTubeしか無く、Xはフォロワー0人＝そのCTAは一度も成立していなかったことへの対応。**サイトはアカウント機能を持たない方針**（`FollowUpdatesCta`のコメント参照）なので、ログインもサーバー保存もせず`localStorage`（`kw_watchlist` / 最終閲覧時刻は`kw_watchlist_seen`、`src/lib/watchlist.ts`）にだけ持つ。端末を変えると消えるが、会員登録という最も高い障壁を置かない方を優先した。Reactからは`useSyncExternalStore`で購読して読む（`useEffect`内でsetStateして同期するとhydration後に一瞬ずれた状態が描画され、eslintの`react-hooks/set-state-in-effect`にも掛かる）。開示の取得は`/api/watchlist?codes=…`（`getHoldingsByStockCode()`を銘柄ごとに引き、1銘柄の失敗は空配列に落として他の銘柄は表示する。コードは英数字4〜5桁のみ通す。CDN 5分キャッシュ）。上限30銘柄。初回訪問（`kw_watchlist_seen`が空）は全件が新着になって意味を持たないためバッジを付けない。保存内容は端末ごとに違い検索結果に出しても中身が無いので`noindex` |
| `/investors` | 投資家一覧。見出し直下にカテゴリ別の絞り込み（`?category=`クエリでSSRフィルタ、各カテゴリの件数を`(N件)`で表示）を配置し、EDINET大量保有報告書を提出したことがある投資家を最終開示日が新しい順に列挙（`edinet_filer_summary` Supabaseビュー経由）。投資家は約2,900件あるため`?page=`で100件ずつのページ送り（カード化で1件あたりの縦幅が増えたため、2026-08-18に200件から他の索引ページと同じ100件へ変更）（クロール可能な素の前後リンク。各ページは自分自身をcanonicalにする）。カードはMUIコンポーネントではなく素の`ul`/`li`＋`.card`＋`DealTypeLabel`（サーバーコンポーネントの軽量ラベル）で描画する（`.card-grid-wide`＝2列相当。投資家名が長く3列だと折り返しが増えるため）。各カードに分類アイコン（`DealTypeIcon`、2026-08-23）。成績のある投資家のカードには4行目に「開示3ヶ月後 平均±X%・勝率Y%（N件）」を出す（`getFilerReturnMap()`＝`investor_returns_3m`ビューを1回で全件取り切って引き当てる。一覧は1ページ100件並ぶため、投資家ごとに`getFilerReturnSummary()`を呼ぶとリクエストあたり数百往復になる。買い開示が`MIN_POSITIONS`件に満たない投資家はビューに載らないので行ごと出ない。2026-08-30追加: 大量保有アラート型の競合アプリは株価データを持たず「誰がいくら買ったか」で止まるため、開示後リターンはこのサイトにしか無い数字だが、それまで`/ranking/returns`と投資家個別ページの中にしか出ていなかった）。一覧の下に「最新の解説記事」＝アイキャッチ付き記事カード4件（`RelatedArticles`、2026-08-22）。**冒頭の直答文＋ファクトボックス（2026-08-29）**: 1ページ目にだけ「大量保有報告書を提出した投資家をN者掲載しています。分類別では◯◯がX者、…の順に多く、直近7日間に新しい開示があったのはY者です（最新の開示日は…）」という1文と、掲載投資家数・最も多い分類・直近7日に開示があった投資家・最新の開示日の`FactBox`を置く。`?category=`で絞り込んでいるときは「『◯◯』に分類される投資家はM者です」に切り替え、タイルも[その分類の人数／全体の人数／直近7日／最新開示日]にする。2ページ目以降は同じ文が並ぶだけなので出さない。PerplexityBotの巡回がこのハブに集中していた（直近14日で28回。`tools/geo_report.py`の実測）のに、ページの答えがカードの一覧の中にしか無かったため。 |
| `/investors/[filer]` | 投資家別ページ。URLは`/investors/<連番ID>`（Supabase `edinet_filer_ids`。2026-08-23まで提出者名そのものをURLにしていたが、日本語・全角・空白入りの長いURLはGoogleに登録されず（カバレッジで603件が未登録）、事前生成時のファイル名長制限にも掛かっていたため番号化）。旧URL（提出者名）で来たら`resolveFiler()`で番号URLへ308転送。リンク生成は全て`investorPath(filerId, filerName)`（`src/lib/investorPath.ts`）経由で、名前→IDは`getFilerIdMap()`（全件1回読み・1時間キャッシュ）か`getFilerIdByName()`で引く。プロフィールの直後に「買い開示の3ヶ月後リターン」パネル（`src/components/FilerReturnRecord.tsx`、2026-08-21追加。平均・中央値・勝率・日経平均比と「198名中◯位」＋`/ranking/returns`への導線。「この投資家についていって大丈夫か」に最初に答える数字なので保有銘柄より前に置く。ランキングと同じ`investor_returns_3m`の行を読むので数字は必ず一致する。買い開示3件未満の投資家はビューに載っていないため非表示＝n=1・2の「実績」は出さない）、その下に「主な保有銘柄」（`holdings`を`issuerCode`で重複排除し保有比率つきで列挙）、「直近で買い増した銘柄／売却した銘柄」の2カラム（銘柄ごとの最新開示を前回比率との差で方向判定し`RatioTransition`付きで列挙。前回比率が取れない開示は方向不明として出さない。SEO/AIO 30日計画P3、2026-08-15追加）、続けて「最近の取引」の見出しでその投資家が開示した保有銘柄・保有比率の推移を一覧表示（`edinet_large_holdings`/`edinet_filer_classification`。開示1件ごとにEDINET原文PDFへの直リンクを持つ＝`/disclosures`廃止に伴い2026-08-18に移設。「3ヶ月後」列は買い増し・新規取得の開示について開示日の終値から63営業日後の終値までの騰落率＝上のパネルの平均値の内訳で、売却の開示・訂正報告書・まだ3ヶ月経っていない開示は「—」。突合は`investor_return_positions_3m`の`doc_id`で行う。(銘柄コード,開示日)で突き合わせると同日に変更報告書と訂正報告書が並ぶケース（実例: 2026-03-18のアーカス×SHIFT）で訂正報告書の行にも数字が付いてしまい、買い判定のルールをTS側に書き写す必要も出るため）。競合の大量保有報告書データベースには無い「投資家を軸にした横断トラッキング」がこのサイトの差別化ポイント。`edinet_filer_classification.profile`（`web/publish_blog_articles.get_filer_profile()`がClaudeの一般知識から800〜1000字程度で生成・キャッシュ）があれば「{投資家名}について」の解説文として一覧の上に表示する。「よくある質問」（`src/lib/investorFaq.ts`の`buildInvestorFaqItems()`が主な保有銘柄・直近の買い増しから自動生成。可視のAccordion＋FAQPage構造化データを同一ソースから出す、2026-08-17追加）の前に「更新を追う」導線（`src/components/FollowUpdatesCta.tsx`＝投資家別RSSと公式Xへのリンク。アカウント機能を持たない方針のため、再訪の受け皿はRSSと公式Xの2つ。2026-08-19追加）。**冒頭の直答文＋ファクトボックス（2026-08-29、`src/lib/investorSummary.ts`）**: h1直下に「◯◯は、EDINETの大量保有報告書でN銘柄・合計M件の保有を開示している投資家です（分類: ◯◯）。直近の開示は…。保有比率が最も高いのは…」という1文と、分類・保有銘柄数・最大保有比率・直近の開示日の`FactBox`を置く。microCMSの手書きプロフィールは無い投資家も多いため、全投資家で必ず出せるDB由来の事実を先に置いている（GEO対策）。売買の内訳は「前回の保有比率と比較できるもの」と断る（新規保有は前回比率が無く買い増しにも売却にも入らないため）。 |
| （共通） | 見出し直下の説明文は1〜2文に短縮し、計算方法・対象条件・出典・免責などの残すべき記述は`src/components/InfoTip.tsx`（ℹ️アイコン、MUI Tooltip。ホバー中またはクリック／タップで開き、外側クリックで閉じる）に格納する（2026-08-23。`/trending`・`/ranking/[slug]`・`/weekly`・`/activists`で適用。スマホで本文より先に長い説明が画面を占有していたため） |
| `/ranking/[slug]` | 投資家ランキング。`returns`=3ヶ月リターンランキング（2026-08-20新設）と`activist`=アクティビストが動いた銘柄の2slug。ヘッダー・フッターの「投資家ランキング」は`returns`が入口。**`returns`**は「この投資家が買った銘柄はその後どうなったか」を出すランキングで、EDINETの買い開示1件を1ポジションとし、開示日（休場なら直後の営業日）の終値から63営業日後の終値までの騰落率を**等ウェイトで平均**した順に並べる（金額加重にすると貸株玉の大きいプライムブローカーが上位を独占して「乗っかる価値」を表さないため）。各行に分類・買い開示件数・勝率・日経平均比（同期間の日経平均の騰落率との差％pt）・最も上がった銘柄を添える。見出し直下に投資家の種類（分類）での絞り込みを`<details>`で畳んで置き（`src/components/InvestorReturnRanking.tsx`、2026-08-22追加。該当0名の分類はボタンごと出さず、各ボタンに`（N名）`を添える）、絞り込むとその分類の中での順位を1位から振り直す（全体順位のまま歯抜けの数字を出すと「アクティビストの中で何番目か」が読み取れないため）。絞り込み中は全行が同じ分類なので分類ラベルは省く。全件（198名）を最初からクライアントに渡してその場で絞り込むので切り替えで追加のfetchは発生せず、`?category=`のようなsearchParamsも使わない（読むとページがリクエストごとの動的レンダリングになりISRキャッシュが効かなくなるため。/trendingの売買方向フィルターと同じ判断）。SSRされるHTMLとItemList構造化データは初期表示＝絞り込みなしの上位30名に揃えている。集計はSupabaseのマテリアライズドビュー`investor_returns_3m`（買い開示1件＝1行の明細`investor_return_positions_3m`を投資家単位に集計したもの。ランキングの数字と`/investors/[filer]`の成績パネル・「3ヶ月後」列を必ず同じ行から作るための2層構成。定義とレビュー観点は`supabase/create_investor_returns_3m.sql`、更新は`tools/refresh_investor_returns.py`＝daily_alert.yml Step 0b）で完結しており、アプリ側は`src/lib/investorReturns.ts`で並べて表示するだけ。開示3件以上の投資家198名（2026-08-20時点）のうち上位30名を表示。3ヶ月が経過していない開示は自動的に対象外になる。**`activist`**=アクティビスト分類の開示（金額降順）だけは「アクティビストが動いた銘柄」でタブにも含めないため銘柄別のまま（集計は`src/lib/rankingStats.ts`の`buildStockRows()`、データ元は`getRecentArticles(30)`）。表示は「1行目=順位＋投資家名（全幅）／2行目=メタ・数値（右端）」の1件1カード（`.card-grid-wide`＝2列相当）。上位30件固定なのでページ送りは無い。`generateStaticParams`で2slugを事前生成、`revalidate=3600`、BreadcrumbList+ItemListのJSON-LD（投資家別ランキングのItemListは投資家ページを指す）とsitemap登録済み。記事詳細ページの「関連ランキング」navからも導線あり。h1はヘッダー・フッターのラベルと揃えた「投資家ランキング」で、個別ランキング名はh2に置く。かつて上部にあった`RankingTabNav`のタブ（3ヶ月リターン／開示急増投資家）は、`/ranking/trending`の廃止で1タブになったため2026-08-21にコンポーネントごと削除した。`buys`（買い増し）・`sells`（売却）は2026-08-20に廃止（直近30日の推定金額を投資家別に合計するだけで、「いちばん大きく張った投資家」が分かっても銘柄選びの手掛かりにならず、常連の運用会社が並ぶだけだった）。`filings`（報告書件数ランキング）は2026-08-18に廃止（当時の`/ranking/trending`と対象がほぼ重複）。`/ranking/trending`（開示急増投資家ランキング）は2026-08-21に廃止（開示件数の「増加分」順は、変更報告書を大量に出す常連が上位に来るだけで、買いか売りか・対象銘柄が分からず次の行動につながらないため。投資家軸のランキングは成績で並べる`returns`に一本化）。いずれもインデックス済みURLなので404にせず`next.config.ts`の`redirects()`で301（buys/sells/filings/旧`/ranking`/trending→すべて`/ranking/returns`。リダイレクトの連鎖は作らない）。両slugとも末尾にアイキャッチ付き記事カード8件（`RelatedArticles`、2026-08-22。件数は2026-08-29に4→8。`returns`は上位30名の投資家による直近記事を1投資家1件まで、`activist`は取得済みの直近30日記事から金額上位を1銘柄1件まで）。`returns`側の取得は`getArticlesByFilerNames()`＝上位30名の提出者名でmicroCMSを絞ってから取る（2026-08-29修正）。それまでは最新50件を取ってから上位30名で絞っていたが、記事は1日25本前後あり50件＝直近2日ぶんしか見ておらず、該当0件でセクションごと消えていた（本番で確認。条件を満たす記事自体は24本あり、上位投資家の直近記事は9日前などにあった）。各カードにアイコン＝`returns`は投資家の分類アイコン（`DealTypeIcon`）、`activist`は銘柄の業種アイコン（`SectorIcon`、sectorは`getCompanyBriefs()`で取得。2026-08-23）。2026-08-29に4ページ共通の視覚強化を実施（データ4ページは「見出し→細いグレーの説明文→小さいカードの羅列」でファーストビューに視線が止まる要素が無く、画像が最下部の記事カードにしか無かったため）: ①各カードの数値に量感バー（`MagnitudeBar`。値/表示分の最大値で幅を出す横バー。1件あたりのHTMLを増やさないようSVGではなくspan2つ＋inline widthで描く。1位のみ金色）②業種アイコンを16px線画→40pxの角丸カラーチップへ（`SectorIcon` `size="lg"`。33業種を資源・素材＝紺／ものづくり＝青／生活・商業・サービス＝金／金融・インフラ・運輸＝濃紺の4グループ色に割り当て。新色は足していない）③記事アイキャッチを「先頭1件が3列中2列ぶんの幅＋最大8件」へ（`RelatedArticles`）（バーは`returns`＝3ヶ月平均リターン〈プラスは緑・マイナスは赤〉、`activist`＝推定金額。カラーチップは銘柄軸の`activist`のみ、`returns`は投資家軸なので`DealTypeIcon`のまま）|
| `/trending` | 銘柄ランキング（2026-08-18に「銘柄・投資家」の2表構成から銘柄のみに変更し「クジラが急増した銘柄」→「取引が急増した銘柄」に改名、2026-08-21にさらに「銘柄ランキング」へ改名しヘッダー上タブ・フッターのラベルも同じ「銘柄ランキング」に揃えた＝隣の「投資家ランキング」と対になる名前にするため。投資家版は`/ranking/trending`へ移設したが、そちらは2026-08-21に廃止）。ページ冒頭の説明文は2文＋注目投資家・FAQへのリンクのみに絞り、NEWの意味・件数と金額の違いなどの補足は`/faq/usage`のQ&Aへ移設。直近7日間にEDINETへ提出された大量保有・変更報告書の件数を、その前の7日間と比べて開示が増えた銘柄を**推定売買金額の大きい順**にランキング（2026-08-27に比較窓を30日→7日、並び順を増加件数順→金額順に変更。「今どの銘柄に大口の資金が入っているか」を今週の動きとして見せるため。7日窓では増加179銘柄のうち151銘柄が「+1件」で並び、件数を先に見ると上位が実質ランダムになっていた＝2026-08-27実測）（銘柄別、件数制限なし＝直近30日で開示が増えた銘柄を全件。上位10件では「急増している銘柄を一覧したい」需要に足りないため2026-08-18に撤廃。増加が1件以上の銘柄は概ね500件弱。`src/lib/trendingStats.ts`＋`TrendingTable`共用コンポーネント。MUI Tableは`minWidth: 420px`で375px幅だと横スクロールが必要だったため、1件1カード（`.card-grid-wide`）に変更し各数値にラベルを添えている。件数制限を外した結果480件・HTML 1.08MB(gzip 106KB)とperf_checkの閾値100KBを超えたため、初回30件をSSRし下端で30件ずつ足すオートページャーにした（`TrendingTable`は`"use client"`。データは最初から全件手元にあるので追加取得のfetchは発生しない）。IntersectionObserverが働かない環境でも進めるよう、sentinelは同時に「もっと見る」ボタンにしてある。ItemList構造化データは初回SSR分の30件のみに揃える）。一覧の上に売買方向の絞り込みボタン「買い／売り／両方」を置いている（2026-08-20追加。`src/components/TrendingDirectionTable.tsx`）。初期表示は**買い**＝保有比率が増えた開示だけの集計で、大口投資家がどこを買い集めているかを見に来る読者が大半のため。売りは保有比率が減った開示、両方は絞り込み導入前と同じ全開示の集計（比率が動かない訂正報告書など方向を判定できない開示は両方にのみ含まれる）。方向の判定は`src/lib/investors.ts`の`holdingDirection()`＝保有比率と前回比率の増減で判定し、前回比率が取れない開示だけ概要欄の語（譲渡・売却・売出・処分）にフォールバックする（トレーディングシステム側`tools/scan_large_holdings.py`の`is_sell_disclosure()`と同じ規律）。3方向の件数は最初から全件クライアントに渡してあるので切り替えで追加のfetchは発生せず、`?direction=`のようなsearchParamsも使わない（読むとページがリクエストごとの動的レンダリングになりISRキャッシュが効かなくなるため）。SSRされるHTMLとItemList構造化データは初期表示＝買いの一覧に揃えている。集計元は記事(microCMS)ではなくSupabase `edinet_large_holdings`（`getHoldingsInRange()`）。記事の蓄積は2026年7月に始まったばかりで「前7日間」がほぼ空になり比較が成立しないのに対し、開示データは1年分あるため今日時点でも意味のある前期間比が出せる。ランキングの並び順は直近7日の推定売買金額の降順で、金額が同じ銘柄（多くは金額を推定できない銘柄）だけ増加件数→直近件数で並べる。各カードは「直近7日 推定◯億円 N件／前7日 …／増加 +N件」の順に出す（金額が並べ替え軸なので先頭に置く。2026-08-22に金額表示を追加、2026-08-27に並べ替え軸へ昇格）。EDINET開示そのものに取引金額の記載は無いので、金額はSupabaseのマテリアライズドビュー`edinet_holding_amounts`（`supabase/create_edinet_holding_amounts.sql`、保有比率の変化幅×発行済株式数×開示日終値の概算＝`web/publish_blog_articles.py`の`estimate_deal_amount_oku()`と同じ式）で開示1件ごとに概算したものを`getHoldingAmountsInRange()`が`doc_id`をキーに返し、TS側で件数と同じバケットに足し込む。売買を伴わない訂正報告書や、株価・発行済株式数が取れない銘柄はビューに行が無い＝0億円扱いで件数にだけ入るため、件数と金額は比例しない（その旨をランキング下の注記と`/faq/usage`のQ&Aに明記）。合計が0の銘柄は「推定0億円」ではなく「金額不明」と表示し、一覧の下側（増加件数順）に並ぶ（2026-08-27実測で直近7日の増加179銘柄のうち63銘柄。金額を読めない銘柄が下に来る理由が読者に伝わるよう、金額欄を空にせず明示する）。金額の取得自体に失敗した場合は全件0億円＝増加件数順にフォールバックしてページは成立させる。金額の再計算はトレーディングシステム側の`tools/refresh_holding_amounts.py`（毎時＋日次）。ランキング表の下にあった「月別の開示件数トレンド」の棒グラフ（`DisclosureTrendChart`＋`getMonthlyDisclosureCounts()`）は、`/weekly`の週次トレンドと役割が重複していたため2026-08-23に削除（コンポーネント・集計関数ごと撤去。市場全体の開示の増減は`/weekly`で見る）。各カードには社名の下に事業内容（`jpx_stock_list.description`、`getCompanyBriefs()`で表示中の銘柄ぶんを一括取得）を添える。社名と証券コードだけでは何の会社か分からず判断できないため（2026-08-18追加）。事業内容が未生成の銘柄は業種（`sector`、ほぼ全銘柄にある）で代替する。事業内容の行数差でカード下端がガタつかないよう、カードを`flex h-full flex-col`で行の高さいっぱいに伸ばし件数行を`mt-auto`で下揃えにしている。事業内容そのものの生成はトレーディングシステム側（`web/publish_blog_articles.py`の`get_company_description()`＝web検索で裏取り、`tools/backfill_company_descriptions.py --recent-days N`で既存銘柄を一括バックフィル）。銘柄ページ(`/stocks/[code]`)は上場銘柄マスターに載っていれば解説記事が無くても存在するため、マスターに無いコード（上場廃止等）だけリンクにせずテキストのまま出す（404へのリンクを作らない）。ランキングの下に「ランキング銘柄の解説記事」＝最新60記事のうちランキング入り銘柄のものを1銘柄1件・8件までアイキャッチ付きカードで表示（`RelatedArticles`、2026-08-22。2026-08-29に4→8件へ増やし、絞り込み後に8件残るよう取得元も30→60記事に広げた）。各銘柄カードに業種アイコン（`SectorIcon`＝JPX33業種の自作SVGラインアイコン。会社ロゴは商標・取得元の問題で使えないため業種で代替、2026-08-23）。2026-08-29に4ページ共通の視覚強化を実施（データ4ページは「見出し→細いグレーの説明文→小さいカードの羅列」でファーストビューに視線が止まる要素が無く、画像が最下部の記事カードにしか無かったため）: ①各カードの数値に量感バー（`MagnitudeBar`。値/表示分の最大値で幅を出す横バー。1件あたりのHTMLを増やさないようSVGではなくspan2つ＋inline widthで描く。1位のみ金色）②業種アイコンを16px線画→40pxの角丸カラーチップへ（`SectorIcon` `size="lg"`。33業種を資源・素材＝紺／ものづくり＝青／生活・商業・サービス＝金／金融・インフラ・運輸＝濃紺の4グループ色に割り当て。新色は足していない）③記事アイキャッチを「先頭1件が3列中2列ぶんの幅＋最大8件」へ（`RelatedArticles`）（このページのバーは推定売買金額）。ヘッダーから常時リンク |
| `/monthly` | 月別アーカイブの入口。記事がある月を新しい順に、開示件数・推定取引金額つきでカード一覧（`src/components/MonthList.tsx`＝`.card-grid`の3列相当、`lib/microcms.ts`の`getAllMonthsForIndex()`）。月カードの下に「最新の解説記事」＝アイキャッチ付き記事カード4件（`RelatedArticles`、2026-08-22）。ヘッダーから常時リンク |
| `/monthly/[month]`（`YYYY-MM`） | 月別まとめ。その月の開示件数・推定金額・買い/売りの要約に続けて、「この月に動いた投資家」（提出者別ランキング上位10件、`lib/weeklyStats.ts`の`buildFilerRanking()`）「この月に狙われた銘柄」（銘柄別ランキング上位10件、`buildStockRanking()`）「注目取引」（金額上位3件の`FeaturedArticleCard`）「日別の記事一覧」（`/date/[date]`へのリンク）と前後月ナビを表示。取引日別ページは日数分だけ増える一方で`/weekly`から張られるのは直近7日分だけで、それより古い日付はサイトマップにしか載らない孤立ページになっていた。この月ハブを親に置くことで、全ての取引日別ページが「ヘッダー→月別アーカイブ→各月→各日」でクロールできるようにしている |
| `/date/[date]`（`YYYY-MM-DD`） | 取引日別の大口投資家の動きまとめ（同一`dealDate`の記事を`-dealAmount`順に一覧表示）。先頭（同日内で金額最大の1件）はTOP/`/weekly`と同じ`FeaturedArticleCard`でページ冒頭にハイライト表示し、残りを一覧グリッドに表示する（2026-08-15、「今日の注目」の編集的ハイライトとして追加）。記事詳細のパンくず（トップ＞日付＞記事）から内部リンクあり。このページ自身のパンくずは「トップ＞{月}＞{日}」で、月は`/monthly/[month]`へリンクする |
| `/about` | 運営者情報・データソース・免責事項（E-E-A-T対策）。投資家分類の用語集（`#dealtype-glossary`）と公式X（@kujira_watch）への導線も含む。meta descriptionは2026-08-23に「運営方針・データソース・免責」だけの26字から、EDINET・5%ルール・13分類のキーワードを含む約110字へ拡充 |
| `/contact` | お問い合わせ（`src/app/(ja)/contact/page.tsx`）。窓口は公式XのDM/リプライ1本（運営者は実名・メールアドレスを公開しない方針。`src/lib/site.ts`の`ORGANIZATION_CONTACT_POINT`と同じ窓口を指す）。掲載内容の誤りの指摘・訂正削除の依頼・引用転載・取材の受け口と、投資助言に当たるため答えられない範囲を用件別に明記する。2026-08-25の監査で`/contact`が404、連絡手段がプライバシーポリシー本文の中にしか無いと指摘されたため追加。フッター・ハンバーガーメニュー・サイトマップからリンク |
| `/privacy` | プライバシーポリシー（AdSense審査の必須要件）。Google AdSenseによる第三者配信広告・Cookie利用とそのオプトアウト方法、アクセス解析（GA4/Vercel Speed Insights/独自アクセスログの`kw_vid` cookie）について記載。ヘッダーのハンバーガーメニューから常時リンク |
| `/faq` | よくある質問のハブ。9カテゴリそれぞれの件数・質問サンプル5件・カテゴリページへのボタンを並べる（回答本文は置かない） |
| `/faq/[category]` | カテゴリ別のQ&A（全508件を9カテゴリに分割）。MUI Accordionで開閉。FAQPage構造化データはそのページに表示しているQ&Aのみを載せる（構造化データと可視コンテンツの一致がGoogleのガイドラインで必須のため）。データ本体は`src/lib/faqData.tsx` |
| `/sitemap.xml` | sitemapindex（実体は`src/app/sitemap-index.xml/route.ts`、`next.config.ts`のrewriteで割り当て）。子サイトマップ`/sitemap/<id>.xml`（pages/stocks/dates/investors/articles の5種、`src/app/sitemap.ts`の`generateSitemaps`）を束ねる |
| `/robots.txt` | `src/app/robots.ts` |
| `/ads.txt` | AdSenseの販売者情報（`src/app/ads.txt/route.ts`）。`NEXT_PUBLIC_ADSENSE_CLIENT`未設定時は404を返す |
| `/feed.xml` | RSSフィード（新着記事20件、`src/app/feed.xml/route.ts`）。ヘッダーのハンバーガーメニュー・`<head>`の`alternate`リンク・`llms.txt`から参照 |
| `/investors/[filer]/feed.xml`・`/stocks/[code]/feed.xml` | 投資家別・銘柄別のRSS（`src/lib/rss.ts`で組み立て、`revalidate = 300`）。記事ではなくEDINET開示そのものを源にするため、記事化されなかった小さな開示も追える。各ページの`<head>`の`alternate`と「更新を追う」導線から参照 |
| `/api/counter` | 累計訪問者数カウンター用。POSTは `increment_blog_visit_counter` RPCで加算、GETは加算せず現在値を返す（ハンバーガーメニュー内表示用） |
| `/api/articles` | 記事一覧のオートスクロール用（GET、`offset`/`dealType`クエリでmicroCMSの次のページを返す） |
| `/api/stocks/search` | ヘッダーの検索用（GET、`q`クエリで銘柄の`results`（microCMS記事の`stockCode`/`stockName`部分一致＋Supabase `jpx_stock_list`の全上場銘柄検索`searchStockMaster()`＝コード前方一致・社名部分一致、コードで重複排除し記事ヒットを先頭に最大20件）と、EDINET提出者名の部分一致`investors`（`getAllFilers()`のキャッシュから最大10件）を返す。投資家の結果は日本語版のみドロップダウンに「銘柄/投資家」のグループ表示） |

### 廃止したURL（`next.config.ts`の`redirects`）

- **英語版 `/en` 配下（2026-08-29に廃止）**: `/en`・`/en/articles/[id]`・`/en/stocks/[code]`・
  `/en/about`・`/en/privacy`・`/en/investors`・`/en/category/[slug]` と、取りこぼし用の `/en/:path*`
  を、対応する日本語ページ（カテゴリとその他はトップ）へ301で飛ばす。廃止の理由は英語面に
  検索流入の山が無かったこと（直近14日の実測: EN記事1,046本にブラウザPV1,544・中央値1・最大7、
  10PV以上は0本。日本語版は最大73PV・10PV以上が42本）と、英訳のぶん記事1本あたりの
  出力トークンが約1.3倍かかり続けること。記事生成側（`web/publish_blog_articles.py` /
  `publish_buyback_articles.py`）も`bodyEn`/`titleEn`を作らなくなった。microCMSに既にある
  `bodyEn`/`titleEn`のデータは消していない（表示しないだけ）。
- 廃止したランキングURL（`/ranking`・`/ranking/buys`ほか）は`/ranking/returns`へ301。
- **`/disclosures`（2026-08-18に廃止）→ `/` へ301（2026-08-29追加）**: 廃止時にリダイレクトを置き忘れて404のまま残っていた。`tools/geo_report.py`の実測でAIクローラーが直近30日に58回・GA4のPVも28日で63件当たっており、AI側に残った参照が全部404を踏んでいた。役割が最も近いTOP（新着開示の日付降順一覧）へ寄せる。
- **削除した記事URL（Supabase `deleted_article_redirects` 経由で308）**: `next.config.ts`ではなく
  記事詳細ページ（`(ja)/articles/[id]/page.tsx`）がmicroCMSで404だったときにだけ
  `src/lib/articleRedirects.ts`で引き当て、`permanentRedirect`する。行き先は重複削除なら
  「残した方の記事」、それ以外は「その銘柄のページ」。低価値・重複・誤報の記事は今後も消すが、
  順位の付いたURLを404で捨てないため。2026-08-29のGSC実測で、検索結果に出ているURL924件のうち
  194件が404を返し、そこに28日で25クリック（全体の18%）が着地していた（うち124件が削除済み記事）。
  登録は削除ツール側（`lib/article_redirects.py`）。過去分257件は`tools/backfill_article_redirects.py`で復元済み。
  **行き先が現在公開されているページかを`resolveArticleRedirect()`で確かめてから308を返す**（`stockHref()`で判定）。
  銘柄ページは記事数などの条件を満たすものだけを公開しているため、記事を消した結果その銘柄ページも
  非公開になる組み合わせがある。そのまま308を返すと308→404の二段になり、素の404より悪い。
  実測2026-08-29: 検索表示のある削除済み記事124件のうち、銘柄ページが公開中で引き継げるのは52件
  （表示265・クリック10/28日）。残る72件は銘柄ページも生存記事も無いため404のままが正しい
  （PR #294で入れた「同じ銘柄の生存記事へ振り替える」フォールバックは該当0件だったのでPR #295で削除済み）。
- **`/articles`（記事一覧の入口としてクローラーが推測してくるURL）→ `/` へ301**: 実在したことは無いが、AIクローラーが14日で18回取りに来ていた（同上の実測）。記事一覧はTOPそのものなので404にせず寄せる。

## 計測・ログ

- **累計訪問者数カウンター**: ヘッダー上部（サイト名の右側、モバイル幅ではロゴの1行表示を優先して非表示・sm以上のみ）に表示（`src/components/VisitCounter.tsx`）。ページ読み込み時に `/api/counter` をPOSTし、Supabaseの `blog_visit_counter`（単一行）をアトミックにインクリメントして返す。スマホでも見られるようハンバーガーメニュー最下部にも置いており、こちらは `increment={false}` でGET（加算なし）を使い二重計上を防ぐ。
- **アクセスログ**: `src/proxy.ts`（Next.js 16で`middleware`から改称された`proxy`規約）が全リクエストのUser-Agentを見て、Googlebot/Bingbot/GPTBot/ClaudeBot/GoogleOther/meta-externalagent/Amazonbot等の既知クローラーは`bot_name`にその名前、主要ブラウザ（Chrome/Safari/Firefox/Edge/Opera）は`bot_name="Browser"`としてSupabaseの `blog_crawler_log` に記録する（`src/lib/crawlers.ts` の `classifyVisitor()`）。curl等のスクリプト・UA不明のノイズはどちらにも一致しないため記録しない。`bot_name`で絞り込めば「本当のクローラー」と「ブラウザからの実アクセス」を区別できる。ただし`BOT_PATTERNS`は個別名の列挙なので新顔のクローラーは取りこぼし、UAに自己申告があっても`"Browser"`に混ざる（2026-08-28の実測でmeta-externalagentが1日1,031PV、Amazonbotが337PVそこに入っていた）。気づいたら`BOT_PATTERNS`に追加すること。取りこぼしは`tools/traffic_report.py`が名前付きで表示する。`bot_name="Browser"`の行には、`kw_vid`という匿名cookie（初回アクセス時にランダムUUIDを発行、個人情報なし）由来の`visitor_id`も記録するため、`count(DISTINCT visitor_id)`でユニーク訪問者数を集計できる。ログはSupabaseダッシュボードのTable Editorから直接閲覧・CSVエクスポートできる。
- どちらも `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`（トレーディングシステム側と同じSupabaseプロジェクト）が必要。未設定でもビルド・記事表示自体には影響しない（カウンターAPI呼び出し時にのみエラーになるが、フロント側は握りつぶして非表示にする）。

- **`/faq`の分割（2026-08-15）**: 以前は全502件のQ&Aを`/faq`1ページに置いており、HTMLが**1.57MB（gzip 241KB）**まで膨らんでいた。内訳は可視HTML 873KB・RSCペイロード 471KB・FAQPage構造化データ 225KBで、**同じ本文が1つの文書に3回**入っていた（クライアントコンポーネント`FaqList`へ全件をpropsで渡すとハイドレーション用にRSCペイロードへ、構造化データで更にもう1回）。さらに可視HTMLは`MuiAccordion-root`が502個・`Mui〜`クラスの出現が14,160回で、**タグを除いた実テキストは220KBだけ＝75%がマークアップ**だった。カテゴリ別ページに分割して解消（gzip後: `/faq` 241KB→18KB、各カテゴリページ22KB）。分割によってタブでの絞り込みが不要になったため`FaqList`は削除した。
- **一覧ページのストリーミング（2026-08-16）**: `/investors`・`/stocks` は `searchParams`（絞り込み・ページ送り）を読むためリクエストごとのdynamic renderingになり、一覧を描き終わるまで最初の1バイトも返せていなかった（本番実測でTTFB 0.43〜0.51秒。静的ページは0.18〜0.20秒）。**`searchParams` の `await` とデータ取得をページ本体から子コンポーネントへ移し、`<Suspense>`で包んだ**。これでパンくず・h1までの「シェル」が即座に流れ、一覧は準備でき次第あとから流れる。フォールバックは `src/components/ListFallback.tsx`（MUIのSkeletonを使うとこの骨組みのためだけにクライアントJSが増えるので素のdivで組んでいる）。Next.js 16はストリーミングメタデータが既定なので、`generateMetadata`が`searchParams`を読んでもTTFBはブロックされない。
- **表示速度の週次チェック**: GitHub Actions `perf_check.yml`（毎週月曜 09:00 JST / 手動実行も可）が `tools/perf_check.py`（リポジトリルート）で本番の代表11ページを計測する。見るのは「初期表示までに待たされる量」に直結する3つ ―― **TTFB**（キャッシュが効いていないと伸びる。実例: `/investors` が`searchParams`でdynamic renderingになりキャッシュ無しで1.6〜1.9秒）、**HTML転送量**（一覧の全件描画で膨らむ。実例: `/investors` が1.5MB）、**`<head>`内のレンダリングブロッキングCSS**（ウェブフォントの`@font-face`で膨らむ。実例: Noto Sans JPで496個・gzip 130KB）。いずれもgzip後の実転送量で、TTFBは3回計測の最小値。JSは初期表示を直接ブロックしないため参考値のみ（外部ドメインの広告・計測タグは自分たちで削れないので集計対象外）。閾値（TTFB 0.8秒 / HTML 100KB / CSS 30KB）を超えたページがあれば`perf`ラベルのIssueを立てて追記し、全ページが閾値内に戻ったら自動クローズする。**閾値は「調べる価値がある」ラインであって目標値ではない**。計測ロジックは`tests/test_perf_check.py`で保護している（HTMLのパースを間違えると「CSSが軽い」と誤判定して肥大化を見逃すため）。

## 広告（Google AdSense）

- 設定は `src/lib/adsense.ts` の環境変数に集約している。**`NEXT_PUBLIC_ADSENSE_CLIENT` が未設定の間は、広告スクリプト・広告枠・`/ads.txt` のすべてが何も出力しない**（ローカル・プレビュー環境では完全に無効）。スロットIDが未設定の掲載位置にも広告は出ないので、片方だけ先に有効化することもできる。
  - `NEXT_PUBLIC_ADSENSE_CLIENT`: サイト運営者ID（`ca-pub-...`）。ローダースクリプト（`src/components/AdSenseScript.tsx`、両ロケールのlayoutに設置）と`<ins>`の`data-ad-client`に使う。`ca-`を除いた値が`/ads.txt`の販売者IDになる。
  - `NEXT_PUBLIC_ADSENSE_INFEED_SLOT` / `NEXT_PUBLIC_ADSENSE_BOTTOM_SLOT`: 掲載位置ごとの広告ユニットのスロットID（`ADSENSE_SLOTS`の`infeed`/`bottom`）。位置ごとに別ユニットにするのはAdSense管理画面で収益を分けて見るため。どちらもAdSense管理画面で「ディスプレイ広告（レスポンシブ）」として作成する（`data-ad-format="auto"` + `data-full-width-responsive="true"` で描画するため、in-feed広告ユニット固有の`data-ad-layout-key`は不要）。
- 広告枠は `src/components/AdUnit.tsx`（`placement`で位置を指定）の1コンポーネントに統一。記事と広告が地続きに見えると誤クリックを誘発しAdSenseのポリシー違反になるため、必ず「広告」ラベルを上に添える。
  - **infeed**: 記事一覧（TOP・カテゴリ別）のオートスクロールの途中。記事カードの間ではなく**取引日グループの区切り**に、`ARTICLES_PER_AD`（=8）件読み進めるごとに1枠挿入する（`src/components/InfiniteArticleList.tsx`）。最後のグループの後ろは読み込み中のsentinelが続くので置かない。
  - **bottom**: コンテンツ末尾に1枠。記事詳細・銘柄別・投資家別・取引日別・月別・週次・ランキング・銘柄一覧・投資家一覧・トレンド・FAQ・`/about`（日英）に設置。一覧が無限に伸びるTOP・カテゴリ別には置かない（末尾に到達しないため）。**`/privacy`にだけは置かない**（ポリシーページは広告なしで保つ）。
- ローダーは React 19 が `async`+`src` の `<script>` を `<head>` に巻き上げるため、layoutのbody内に置いても審査コードの検出は通る。広告ブロッカー等で `adsbygoogle.push()` が失敗してもページの描画は止めない（例外は握りつぶす）。
- プライバシーポリシー（`/privacy`）はAdSense審査の必須要件。広告ツール・解析ツールを増減させたらこのページの記載も合わせて更新すること。

## SEO/AIO対策

- **metadata**: `src/lib/site.ts` の `SITE_URL`/`SITE_NAME` を起点に、ルートレイアウトで `metadataBase`・タイトルテンプレート（`%s｜${SITE_NAME}` の順＝固有名を先頭にする。全ページのtitleが同じ11字で始まると検索結果で見分けがつかず、Googleにタイトルを書き換えられやすいため）。**記事詳細だけはサイト名サフィックスを付けない**（`title: { absolute: article.title }`）。記事タイトルは銘柄名・提出者名・保有比率だけで既に40〜60字あり、検索結果に出る約32字にサイト名は入らない。2026-08-29のGSC実測で、8位前後・表示10〜53回でクリック0の記事が16本あった・OGP・Twitter Card・`robots` を設定。記事詳細・カテゴリ別一覧は `generateMetadata` で動的に title/description/canonical/OGPを生成する。
- **アイコン/OGP画像/ロゴ**: `src/app/icon.tsx`（ファビコン。`generateImageMetadata`で32/192/512pxの3サイズを`/icon/32`等として生成し、HTMLページの`<head>`にのみ`<link rel="icon">`として注入される。Next 16では`id`がPromiseで渡るので`await`する）・`src/app/apple-icon.tsx`（iOS「ホーム画面に追加」用の180x180、`<link rel="apple-touch-icon">`。2026-08-23追加＝それまで32pxのiconしか無くホーム追加時に引き伸ばされてぼやけていた。iOS側で角丸が付くので正方形で塗る）・`src/app/manifest.ts`（`/manifest.webmanifest`。Android Chromeやアプリ内ブラウザは manifest が無いと「ホーム画面に追加」を出さない／失敗するため2026-08-23追加。`display: standalone`、アイコンは`/icon/192`・`/icon/512`・`/apple-icon`を指す）・`src/app/(ja)/opengraph-image.tsx`（SNSシェア用1200x630。**appルート直下に置くと`(ja)`ルートグループ内ページの`<head>`に`og:image`/`twitter:image`が一切注入されない**＝Xカードのサムネが出ない不具合が実際に起きたため、必ずルートグループ内に置くこと。`icon.tsx`はルート直下でも全ページに効くが、OGP画像はルートレイアウトを持つルートグループの境界を越えない）・`src/app/logo/route.ts`（構造化データ用の正方形512x512ロゴ、`/logo`）は `next/og` の `ImageResponse` でクジラ絵文字🐋をブランドネイビー背景に合成して動的生成（画像アセット不要）。`logo`はOGP画像と違い横長ではなく正方形にしてある（構造化データの`logo`にはOGP用の横長比率ではなく正方形〜近い比率の画像を指定するのがGoogleの推奨のため）。加えて`src/app/favicon.ico`（同デザインの静的PNG内蔵ICO、16/32/48/64px）を配置している。Next.jsは`favicon`をコードから生成できず画像ファイルが必須なため、`icon.tsx`だけでは`/sitemap.xml`・`/robots.txt`・`/feed.xml`のような`<head>`を持たないルートを直接開いたときにブラウザが`/favicon.ico`にフォールバックし、ファイルが無いとVercelの既定favicon（三角ロゴ）が表示されてしまう。静的ファイルを置くことでサイト全体のフォールバック先を統一している。
- **運営者情報（E-E-A-T）**: 運営者は実名を公開しない方針（2026-08-23決定）。代わりに`/about`の「運営者について」セクションに個人運営・X（`X_HANDLE`）・AI生成＋運営者確認の体制・連絡窓口（XのDM/リプライ）を書き、`Organization`構造化データに`contactPoint`（`ORGANIZATION_CONTACT_POINT`＝公式X）を宣言する。記事の`author`は実態どおり`Organization`のまま（`Person`は併記しない）。
- **フッターの見出し**: 各リンク群の見出し（主要ページ／一覧・アーカイブ／サイト情報／フォロー）は`<p class="kicker">`で、`h2`にしない。本文にH2の無い一覧ページでフッターがページの見出し構造として拾われるため（2026-08-23）。
- **セマンティックなマークアップ（2026-08-30）**: 記事カード（`ArticleCard`/`FeaturedArticleCard`）は `Card component="article"` で `<article>` として出す（カード1枚が単体で意味の通る記事の要約であるため）。見出しは置かれる文脈に応じて段を下げる: `ArticleCard` の `headingLevel`（既定`h2`）と `DealDateHeading` の `level`（既定`h2`）、`InfiniteArticleList` の `dateHeadingLevel`（既定`h2`、カード見出しは常にその1段下）で指定し、TOPは「新着の取引」h2 →取引日 h3 →記事 h4、`/stocks/[code]` も同じ入れ子、`RelatedArticles`・記事詳細の関連記事はセクションh2の下なので記事 h3、`/date/[date]` は取引日自体がh1なのでカードは既定のh2。**既定値は変更前の出力と同じ**にしてあるので、明示的に渡していないページのHTMLは変わらない。導入前のTOPは記事カードが全部`<div>`で見出しが`h1:1/h2:33/h3:1`＝セクション見出しも取引日の区切りも記事タイトルも同じh2に潰れており、アウトラインからページの構造が読めなかった（導入後は`<article> 30`・`h1:1 h2:6 h3:2 h4:27`）。記事カードの並びは`<ul>`/`<li>`で出す（TOPの注目枠・取引日グループ・`RelatedArticles`・`/date/[date]`・`/articles/[id]`の関連記事・`/stocks/[code]`・`/monthly/[month]`の注目枠の7か所）。**liには`className="grid"`を付けること**＝liを挟むとグリッドの升目がカードからliに移るため、`flex`だと子がコンテンツ幅までしか広がらずカード幅が潰れる（実測2px）。`grid`なら単一の子が升目いっぱいに伸びるので、カードの`height:100%`も同じ行での高さ揃えもそのまま効く。中黒とインデントはTailwind v4のpreflightで消えており、記事本文の`.prose`の外なのでtypographyプラグインのリスト装飾も掛からない。なお**出力HTMLが1行になるのはReact SSRの仕様で直せない**（本番564KBで改行0）。ソースを読むときはDevToolsのElementsか整形ツールを通す。
- **構造化データ (JSON-LD)**: ルートレイアウトに `WebSite`/`Organization`（`logo`に`/logo`を指定。`alternateName`＝`SITE_ALTERNATE_NAMES`（「大口投資家の監視ブログ」「クジラウォッチ」「kujira-watch」「Big Investor Watch」から現在の`name`を除いたもの）・`description`・`sameAs`＝`ORGANIZATION_SAME_AS`（公式X・YouTube）を2026-08-23に追加。サイトの呼び名が3表記で揺れていてもAI検索エンジンに1エンティティとして認識させ、公式アカウントをエンティティグラフに結び付けるため）。記事詳細には `Article`（`headline`/`url`/`author`＝サイト運営組織/`publisher`＋`publisher.logo`/`image`＝アイキャッチ/`about`に銘柄名・証券コード/`citation`に出典URL）と `BreadcrumbList`（トップ＞取引日＞記事タイトル。取引日は`/date/[date]`へリンク）。トップ・銘柄別・カテゴリ別・取引日別・週次まとめの各一覧ページには `ItemList`（各`itemListElement`に`name`＝記事タイトルを含める）と `BreadcrumbList`（トップ以外）、FAQページには `FAQPage` を埋め込み。Google/AI Overview双方の情報抽出を想定。
- **公開日・更新日の機械可読化（2026-08-30）**: 診断ツールから「公開日・更新日が機械可読になっていない」と指摘されたため、記事以外のページにも日付を持たせた。可視の日付は`<time datetime="YYYY-MM-DD">`（`toDateAttr()`でISO日付へ揃える。microCMS/Supabaseの日付は`2026-08-29`と`2026-08-29T00:00:00.000Z`が混在する）、同じ値を`WebPage`の`datePublished`/`dateModified`として出す。共通実装は`src/components/DataUpdatedAt.tsx`（可視の1行＋JSON-LD。JSON-LDだけ欲しいページは`PageDatesJsonLd`）と`src/lib/format.ts`の`latestDateOf()`。**日付にはビルド時刻を使わず、そのページが実際に反映しているデータの最終日を渡す**（毎日ビルドしても中身が変わらない日があるため。サイトマップの`<lastmod>`と同じ規律）。適用先はTOP・`/articles`・`/category/[category]`・`/monthly`・`/monthly/[month]`・`/date/[date]`・`/weekly`・`/trending`・`/stocks`・`/stocks/[code]`・`/investors`・`/investors/[filer]`・`/activists`・`/buybacks`・`/ranking/[slug]`・`/about`。`/ranking/returns`だけは行の`latestBuyDate`が常に約3ヶ月前になるため、`getLatestReturnCohort().date3m`＝3ヶ月後リターンを最後に算定した日を使う。`/about`はmicroCMSのaboutオブジェクトの実`updatedAt`。更新日を追跡できない静的ページ（`/privacy`・`/terms`・`/faq`・`/contact`）はサイトマップの`<lastmod>`と同じ理由で日付を出さない（不正確な日付を出すほうが害が大きい）。
- **サイトマップ**: 全記事・カテゴリ・銘柄別・月別（`/monthly/[month]`、日別より高い優先度）・取引日別・投資家別ページと、集約ページ（`/weekly`・`/trending`・`/monthly`）を含む。URL総数が6,000件超に増えたため、`/sitemap.xml`はsitemapindexとし、`src/app/sitemap.ts`の`generateSitemaps`で種類別の子サイトマップ`/sitemap/<id>.xml`（ID一覧は`src/lib/site.ts`の`SITEMAP_IDS`）に分割。子はビルド時ではなくリクエスト時に生成（`dynamic = "force-dynamic"`。microCMSの一時的な障害でVercelのビルド自体が失敗しないようにするため）だが、データ取得は`unstable_cache`で1時間キャッシュする（英語版があった頃は英訳本文全文を毎リクエスト全件取得しており応答が9秒超だった）。`app/sitemap.xml/route.ts`という配置はmetadata予約名とのルート衝突でビルドできないため、実体は`sitemap-index.xml`でrewrite割り当てにしている。`<lastmod>`は「そのページの内容が実際に変わるデータ源」から厳密に取る（記事=updatedAt、投資家=最終開示日、銘柄/日別/月別/カテゴリ=該当する記事群の最新updatedAt、記事一覧系ハブ=全記事の最新updatedAt、/activists・/investors=最新開示日、/about=microCMSのaboutオブジェクトのupdatedAt）。更新日を追跡できない静的コンテンツ（/privacy・FAQ）はlastmod自体を省略する（不正確なlastmodはGoogleに信用されなくなるため）。同一「銘柄×提出者」で最新1本以外の記事（`supersededArticleIds()`）は記事ページ側で`noindex, follow`にしているため、`/sitemap/articles.xml`からも外す（載せたままだと「サイトマップに載っているのにnoindex」を送ることになる）。`<changefreq>`と`<priority>`はGoogleが公式に無視すると明言している値なので出さない。全エントリはloc/lastmodのみの統一形式（sitemaps.org公式XSDのスキーマ検証も全ファイル通過済み）。
- **集約ページの公開対象の絞り込み（`src/lib/pageIndexability.ts` / `src/lib/publishedPages.ts`）**: 2026-08-24にAdSenseの審査へ「有用性の低いコンテンツ」で不承認になった際の実測で、全4,756URLのうち75%が自動生成の集約ページであり、取引日別223件は記事1件だけが87件（本文中央値303字）、投資家別2,972件は解説文が無いものが2,152件・開示1件だけが1,002件、銘柄別664件は記事1件だけが482件と、Googleの「質の低いコンテンツ」ガイドラインが挙げる cookie cutter pages に該当していた。閾値は`isIndexableDatePage()`＝記事3件以上、`isIndexableInvestorPage()`＝開示2件以上かつ投資家の解説文あり、`isIndexableStockPage()`＝記事2件以上、または記事1件以上かつ事業内容の説明あり。**2026-08-29に、条件を満たさないページは`noindex`ではなく404にした**（オーナー判断）。noindexは「評価しないでくれ」という指定でしかなくURLは存在し続けるためで、実測で銘柄2,355・投資家2,417・取引日197＝約4,969URLがnoindexのまま残っていた。ページ側は`notFound()`、サイトマップは元から除外済み。**404にする以上、内部リンクも同じ判定で出し分ける**（リンクだけ残すと記事・一覧・ランキングから大量のリンク切れが出る）。そのための公開集合とhref解決を`src/lib/publishedPages.ts`に置き、`getPublishedStockCodes()` / `getPublishedDates()` / `getPublishedFilerNames()` と`stockHref()` / `dateHref()` / `investorHref()`（公開していない対象はnullを返すので、呼び出し側はリンクにせず素のテキストで出す）を全リンク箇所で使う。銘柄一覧・投資家一覧・ヘッダーの検索API（`/api/stocks/search`）も公開対象だけを返す。RSS（`/stocks/[code]/feed.xml`・`/investors/[filer]/feed.xml`）もページと同じ条件で404にする。集合は`unstable_cache`に入れない（Setは直列化で`{}`になる。2026-08-25にこれで本番ビルドが24時間落ちた）。**判定は必ず`pageIndexability.ts`に集約すること**（ページ側・サイトマップ側・内部リンク側で条件がずれると「サイトマップに載っているのに404」「リンクを踏むと404」という矛盾を送ることになる）。
- **インデックス対象の絞り込み（`src/lib/articleIndexability.ts`）**: 記事は2段階で絞る。①`isIndexableArticle()`＝推定金額3億円以上 **または** 保有比率の変化1pt以上（生成側`web/publish_blog_articles.py`の`INDEXABLE_MIN_*`と必ず同じ閾値にする。**新規記事の足切り`is_worth_publishing()`は2026-08-29に5億円/1.5ptへ引き上げたが、こちらは据え置き**＝既に順位が付いている既存記事をnoindexに落とさないため。新規記事は足切り≧index基準なので「サイトマップに載っているのにnoindex」は起きない）。②`supersededArticleIds()`＝同一「銘柄×提出者」のグループで最新の開示1本だけを残し、それ以前は`noindex, follow`＋sitemap除外（カニバリゼーション対策、2026-08-19導入）。EDINETは同じ提出者が同じ銘柄について変更報告書を何度も出すため、全部インデックスさせると「提出者名」「銘柄名＋大株主」という同一クエリに自サイトの記事が何本も並び、評価が分散してどれも順位が上がらない。過去の開示は`/stocks/[code]`の「保有比率の推移」と`/investors/[filer]`から辿れるため情報は失われない。判定対象は①を通った記事だけにする（足切り済みの記事を「最新」に選ぶとグループ内の唯一価値ある記事まで巻き添えになる）。グループ化には`filerName`が必要で、2026-08-15より前の記事は未設定だったため`tools/backfill_article_filer_name.py`でEDINETから逆引きして補完済み（791本中326本→13本）。実測: index対象 791本 → 710本。
- **直答文とファクトボックス（GEO、2026-08-29）**: 主要ページの見出し直下に「そのページのタイトル＝想定クエリにそのまま答える1文」と、`FactBox`（`src/components/FactBox.tsx`＝主要な数値を`<dl>`で構造化して並べるボックス）を置く。AI検索・強調スニペットは見出し直下の短い断定文と定義リストの数値を抜くため、一覧・表の中にしか答えが無い状態を避ける。適用先は`/stocks/[code]`（大株主が誰か）・`/investors/[filer]`（何を持っているか）・`/activists`（今どこを狙っているか）・`/weekly`（今どれくらい動いているか）・`/investors`（どんな投資家が載っているか。2026-08-29追加）。文面はすべてDBの集計値から組み立てる（`src/lib/stockSummary.ts` `investorSummary.ts` と各ページ内の集計）ので、LLM呼び出し＝APIコストもハルシネーションもゼロ。数値には必ず基準日（開示日・集計期間）を添える（保有比率は開示のたびに変わるため、日付の無い数字を引用されると誤りになる）。JSXの改行は半角スペースになって日本語の文中に入るので、直答文は文字列として組み立ててから流し込む。
- **GEO/AIO向け**: `public/llms.txt` にサイトの目的・一次情報源・更新頻度・主要パス（そのページで答えられる質問つき）・引用時の表記と日付併記のお願い・免責を平文で置く。ページを増やしたらここも更新する。ただし**AIクローラーが`/llms.txt`を取りに来た記録は2026-08-29時点でゼロ**（`blog_crawler_log`実測。ヒット5件はすべてブラウザ）なので、llms.txtに時間を掛けるより実ページ側の直答文・ファクトボックス・出典ブロックを優先する。AIの巡回と流入の実績は `python3 tools/geo_report.py` で見る。
- **再訪導線（登録不要）**: 銘柄ページの「この銘柄をウォッチ」→`/watchlist` で、前回開いたあとに出た開示だけに「新着」が付く（`src/lib/watchlist.ts` / `src/components/WatchStockButton.tsx` / `WatchlistView.tsx`、`localStorage`のみ・アカウント不要）。ヘッダーメニューとフッターの「フォロー」欄では、SNSより先にこれを置く（GA4実測でユーザー364人中363人が初訪問＝再訪がほぼ無く、公式Xはフォロワー0人でフォローCTAが一度も成立していない。登録という最大の障壁を挟まない導線を先頭にする。2026-08-27）。効果は`python3 tools/ga4_clicks.py` のCTA別クリック（label=「この銘柄をウォッチ」）で検証する。
- **回遊導線（2026-08-27、GA4のクリックログ実測にもとづく）**: 28日間の実測で、データ/一覧ページは940PVのうち**889＝95%が内部到達**・滞在75秒と最長なのに入口はわずか51、一方で記事は入口160（TOPに次ぐ2位）なのに滞在16秒、カテゴリページと日付ページは**入口の直帰率100%**だった。ページの価値ではなく「そこから次へ行けるか」が欠けていたため、3か所に導線を足した。①記事詳細のファクトボックス直後に`ArticleNextStep`（銘柄の大株主構成／提出者の保有銘柄／その日の全開示）。本文下の回遊導線は16秒では到達しないため、読み切らなくても見える位置に置く。送り先の銘柄ページは入口直帰13.7%で全種別最良。②カテゴリ・日付ページの末尾に`ListPageNextStep`（カテゴリは`CategoryFilterDetails`での分類乗り換えも併置）。③データ/一覧ページ8枚（trending・ranking/[slug]・weekly・activists・buybacks・investors・stocks・monthly）の末尾に`siblingDataPages()`（`src/lib/nav.ts`）で兄弟ページを3件。**自分の次のページから順に拾う**ので、どのページの末尾も同じ顔にならない（ヘッダーのタブに全部並んではいるが、TOPへの内部到達が398件＝他ページからTOPへ戻る往復が多かった）。効果は`python3 tools/ga4_clicks.py`のCTA別クリック（label）と、日次ログレビューの「## 回遊所見」で追う。
- **データ/一覧ページの<title>（2026-08-27）**: H1・パンくずは短いラベルのまま、検索結果に出す`<title>`だけ`metaTitle`として検索語を入れた形にしている（`/trending`＝大量保有報告書が増えた銘柄ランキング、`/investors`＝大量保有報告書の提出者一覧、`/stocks`＝大量保有報告書が出た銘柄一覧、`/monthly`＝大量保有報告書の月別アーカイブ）。説明文には既に検索語が入っているのに`<title>`が「銘柄ランキング」のような内部呼称のままで、GA4実測でこれらのページは入口51件＝検索から直接来ていなかったため。サイト名を足した実長を**全角30字以内**に収める（超えると検索結果で末尾が切れる）。**長さは必ず本番のサイト名で数えること**: `SITE_NAME`は`NEXT_PUBLIC_SITE_NAME`未設定時の既定値「大口投資家の監視ブログ」（`｜`込みで12字）だが、ローカルの`.env.local`には`NEXT_PUBLIC_SITE_NAME=クジラウォッチ`（同8字）が入っているため、手元のHTMLで数えると4字短く出る。本番実測は/trending 30字・/monthly 27字・/stocks 26字・/investors 25字。※SEOの反映には数日〜数週間かかるので、デプロイ直後に順位で判定しない。
- **公式Xへの導線**: 再訪のきっかけとしてフォローしてもらうため、①記事詳細の本文下にフォローCTA（`src/components/FollowCta.tsx`、ネイビー地バナー＋フォローintentリンク）②ハンバーガーメニューに公式Xへの外部リンク（`MenuLink`の`external`）③`/about` 冒頭にテキストリンク、を置く（定数は`src/lib/site.ts`の`X_SCREEN_NAME`/`X_PROFILE_URL`/`X_FOLLOW_URL`）。④全ページとすべての記事詳細のTwitter Cardに`twitter:site`/`twitter:creator`＝`@kujira_watch`（`X_HANDLE`）を入れる（2026-08-19追加。これが無いと、サイトのURLがXで共有されてもカードにアカウント名が出ず、共有のたびに得られるはずの露出とフォロー導線を捨てることになる）。**公式YouTube（1分ショート解説、`YOUTUBE_CHANNEL_URL`）への導線**も同様に、FollowCta内のアウトラインボタンとハンバーガーメニューの外部リンクとして置く（2026-08-16、SNS評価サイクルで追加。チャンネル側からサイトへは説明文でリンク済み）。X公式の埋め込みタイムライン（`platform.twitter.com/widgets.js` + `twitter-timeline`）も一度試したが、X側のsyndication制限でツイートが描画されずフォールバック文言だけが残るうえ、`widgets.js`と2つのiframeの読み込みでページが体感で遅くなったため2026-08-15に撤去した（再挑戦する場合はこの2点を先に検証すること）。
- **E-E-A-T**: `/about` にデータソース・算出方法・免責事項を明記し、ヘッダーのハンバーガーメニューから常時リンク。「情報源について（EDINET）」節でEDINET書類検索・EDINET API仕様書への外部リンクを掲載し、一次情報を読者が自分で検証できるようにしている（日英とも実装、EN側の文言は`src/lib/i18n.ts`の`aboutSource*`）。
- **週の区切り**: 週別集計はいずれも**月曜始まり**の暦週。「今日」は`toLocaleDateString("sv-SE", { timeZone: "Asia/Tokyo" })`で日本時間で判定する（UTCのまま`new Date()`を使うと月曜0〜9時（JST）はUTCでまだ日曜で、今週が前週として扱われ最新週がグラフから消える＝2026-08-21に是正）。
- **週次トレンドページ**: `/weekly`（`src/app/(ja)/weekly/page.tsx`、`lib/microcms.ts`の`getRecentArticleDigests()`）が週別推移のグラフを表示。「大口投資家の動きを教えて」等の包括的なクエリに個別記事より直接答えられるページとして新設し、ヘッダーから常時リンク・サイトマップに高優先度で登録。週別推移は売買金額8週（インラインSVGの棒グラフ）と、投資家分類ごとの買い/売り棒グラフ（直近6週・`CategoryTrendGrid`）。集計は`getRecentArticleDigests()`が返す`dealDate`/`dealAmount`/`tags`/`dealType`（配列で入っている記事があるため`normalizeDealType()`で正規化）だけを使い、`tags`の売り判定（`isSellArticle()`）と`dealType`を軸に機械的に集計するのみで、解釈や予測は行わない（直近7日の件数・金額タイルは2026-08-22に削除。`buildWeeklySummary()`は`/monthly/[month]`でのみ使用）。個別記事への導線はページに持たせず（日別一覧は2026-08-22に削除）、取引日別ページへは`/monthly/[month]`から辿る。
- **FAQPage構造化データ**: `/faq`（独立ページ、計508問）にFAQPage JSON-LDを付与。「大量保有報告書のきほん」（5%ルール・提出期限・保有目的・共同保有者・特例報告など制度の基礎、22問）「用語・投資家分類」（クジラ・13分類それぞれの解説・議決権・EDINET等の用語、20問）「投資用語の補足」（時価総額・ROE/ROA・信用取引・IPO・TOB用語・運用スタイル等、基礎カテゴリでは扱っていない一般的な株式・金融用語のグロッサリー、110問）「制度・法律の深掘り」（5%ルールの細則・共同保有者/特別関係者の認定・課徴金制度・TOB規制の細目・インサイダー規制・コーポレートガバナンス関連法制など、基礎カテゴリより踏み込んだ制度解説、106問）「投資家分類・業界プレイヤー解説」（年金基金・ヘッジファンド類型・PE/VC・ファミリーオフィス・事業会社など、13分類バッジより粒度の細かい投資主体の解説、105問）「投資基礎の周辺知識」（証券口座・NISA・注文方法・分散投資・税金・チャート/ファンダメンタルズの基本など、初心者向けの実践的な周辺知識、105問）「サイトの使い方」（金額規模の算出方法・検索・会社情報カード・銘柄別ページ・週次トレンド・EDINET原文PDFの在り処・銘柄ランキング・3ヶ月リターン・アクティビスト注目銘柄等、15問）「読み方・活用法」（大量保有報告書は株価にどう影響するか等、投資初心者・中級者向けの実践的な疑問、21問）「運営・データについて」（海外投資家の扱い・同一銘柄で記事が複数ある理由・データの更新タイミング、3問）の9カテゴリに分類。投資家分類の判定方法・記事の作成方法・タグの意味・カテゴリ別/取引日別ページの説明・投資助言でない旨・免責事項・記事の引用可否・広告掲載の有無など、サイト運営に関する説明的・定型的な質問は`/about`の免責事項セクションや各機能ページ自体の分かりやすさで代替できると判断し`/faq`からは割愛しているほか、使用技術スタック・アクセス解析ツール・利用AIベンダー・トレーディングシステムとの内部関係、レスポンシブ対応やページネーション廃止といった自明・些末なUI仕様、既存の質問と内容が重複するものなど、読者にとって価値が低い質問は含めていない。`src/components/FaqList.tsx`（クライアントコンポーネント）で、各質問に色分けされたカテゴリバッジ（ドット+ラベル、カテゴリごとに固定色）を表示するほか、タブUIでも絞り込み表示できる。タブの初期状態は必ず「すべて」（全件表示）にしているため、SSR済みHTMLには常に全問が含まれ、クローラー・AIOが辿れる内容はJSの実行有無によらず変わらない。回答文はAI Overview等でそのまま引用されても意味が通じるよう、質問文を読まなくても完結する1〜3文の自己完結型の文章にしている。可視コンテンツと構造化データの回答文は一言一句一致させている。`/about`からはリンクのみで誘導。

## 実装メモ

- **Supabaseの1000行上限**: PostgRESTは1リクエスト既定1000行で打ち切り、返る行の順序も保証しない。`src/lib/investors.ts`の`PAGE_SIZE`/`MAX_PAGES`で並び順を固定したページングを行い、全件必要なクエリ（`getAllFilers()`＝投資家2,938件、`getHoldingsInRange()`＝開示60日で2,906行）を取り切る。2026-08-15にこの上限で2件の実害を確認して修正した: ①`/investors`一覧とサイトマップに投資家が1,000件しか載らず、約1,900件の投資家ページがサイトマップから漏れていた ②`/trending`の投資家集計が（打ち切られた1000行に対象期間の行が入らず）まるごと空になっていた。
- **一覧ページの重さ**: `/investors`は全件（約2,900件）を1ページに並べていたためHTMLが1.5MBに達し、本番のTTFBが1.6〜1.9秒だった。1ページ200件のページ送り＋行の軽量化（MUIの`ListItem`/`Chip`/`Tooltip`をやめて素の`ul`/`li`＋`DealTypeLabel`）でHTML約390KB・TTFB 0.03〜0.06秒（`unstable_cache`ヒット時）に改善。分類の色定義は`src/lib/dealTypeInfo.ts`の`DEAL_TYPE_COLORS`に置き、`DealTypeBadge`（Chip+Tooltip、記事カード用）と`DealTypeLabel`（一覧用）で共有する。
- **microCMSの全件取得**: 公式SDKの`getAllContents`はページ間に1秒の固定スリープが入るため使わず、1ページ目の`totalCount`から残りのoffsetを割り出して並列取得する（`fetchAllPagesParallel()`＝銘柄一覧・月別インデックス、`fetchAllArticlesByFilter()`＝`/weekly`・`/monthly/[month]`）。直列だと1往復200〜300msがページ数だけ積み上がる。

- 一覧・カテゴリ別一覧は初回表示分（`INITIAL_ARTICLES_COUNT`＝30件）のみサーバー側で取得し、以降は`src/components/InfiniteArticleList.tsx`（クライアントコンポーネント）が画面下端の要素を`IntersectionObserver`で検知して`/api/articles`から次の10件を都度取得・追記するオートスクロール方式。ページネーションのUIやURLの`?page=`は廃止した。初回件数を10→30に引き上げているのは、オートスクロール分（JS実行後にのみ取得される）はクローラーが辿れない実リンクになるため、クロール可能な記事数の下限を底上げする狙い（クロールログで新着記事の巡回が10件相当に留まっていたための対策）。
- `/api/articles`が返す一覧・初回表示分ともmicroCMSへの `fetch` は `next: { revalidate: 60 }` を指定しており、Next.jsのData Cacheが60秒間キャッシュ・再検証を行う（App RouterにおけるISRの実体）。
- 記事詳細（`/articles/[id]`）は動的APIを使わないため `export const revalidate = 86400` をルートセグメントに設定し、オンデマンドISRとして動作する（記事本文は公開後ほぼ不変。60秒だとクローラーのアクセスがほぼ毎回再生成に当たりTTFBが悪化していたため2026-08-18に1日へ延長）。`/stocks/[code]`・`/investors/[filer]`も同じ理由で`revalidate = 86400`（データ源のEDINET開示・会社情報は日次更新のため1日で足りる。2026-08-22に300秒から延長）。事前生成（`generateStaticParams`）は`/articles`直近30件・`/stocks`上位30銘柄・`/investors`開示件数上位30名・`/date`直近7日（2026-09-03に200/300/300/60から縮小。4ルート合計937ページの事前生成にビルド6.6分＝1回8分かかり、Proプランのビルド課金の主因だったため）で、事前生成外のパラメータも2回目以降はCDNから返る（ルート全体がISR扱いになるため）。
- 本文（リッチエディタのHTML）は `dangerouslySetInnerHTML` + Tailwind Typography(`prose`)で描画。記事詳細ページは描画前に`linkifyFilerNames()`（`src/lib/format.ts`）で本文中の投資家名（初出のみ）を`/investors/[filer]`へのリンクに変換する。投資家名はCMS側に構造化フィールドが無く自由記述本文の一部でしかないため、`getFilersByStockCode()`でこの銘柄の開示実績がある投資家名一覧を取得し文字列突合する。EDINETのXBRLは提出者名を全角（`Ｏａｓｉｓ　Ｍａｎａｇｅｍｅｎｔ…`）で保持する一方、AI生成本文は半角で書くため、NFKC正規化した文字列上で位置を探し、本文側の表記（半角）はそのまま残しつつリンク先だけDB上の正式表記（全角）でエンコードする。
- `eyecatch`（アイキャッチ画像）はカード一覧・ヒーロー枠・記事詳細で表示する（未設定の記事はテキスト中心のレイアウトにフォールバック）。記事詳細では`generateMetadata`のOGP画像としても使う。
- **デザインシステム（値の唯一の台帳）**: 色・文字階調・タイポグラフィ・角丸・エレベーション・モーションのトークンは `kujira-watch/docs/design_system.md` に台帳化してある。実体は `src/app/globals.css`（CSS変数＋Tailwindユーティリティ）と `src/theme.ts`（MUI）で、**両者は必ず対で更新する**（片方だけ変えるとTailwindで組んだ画面とMUIで組んだ画面が別デザインになる）。コンポーネントに色・サイズ・角丸・影を直書きせず、先に台帳へ足すこと。**遵守は `python3 tools/check_design_system.py` で機械検査する**（`ci.yml` の `design` ジョブがPRとmainへのpushの両方で走るので、違反はマージ前に止まる。テスト本体は重いのでpush時のみ）。任意値ユーティリティ（`text-[11px]` 等）・Tailwind標準色・`text-foreground/NN`・`.tsx`内のfontSize直書きを検出したら落ちる。文字色は用途で選ぶ4段（`text-ink` / `-secondary` / `-tertiary` / `-muted`）で、以前の `text-foreground/30〜80` の6段は2026-08-30に廃止した（`/50`=3.3:1・`/40`=2.6:1 がWCAG AA未達だったため。MUIの `text.secondary` / `text.disabled` も同じ4段に接続済み）。バッジは `src/components/DotBadge.tsx` が唯一の実装で、分類・売買方向・保有目的・カテゴリ導線の4種はすべてこれを使う（書式は `theme.ts` の `MuiChip` 既定）。カードを影で浮かせるかは `--card-elevation` の1箇所で切り替える（既定は `none`＝罫線区切りのエディトリアル意匠）。投資家分類14種・保有目的5種・FAQカテゴリ9種の**分類色は同じ1つのOKLCHパレット**から選ぶ（ドット= L 0.63/C 0.15、文字= L 0.46/C 0.13。文字は背景トークン3種すべてでWCAG AA達成、最小は`--section-tint`上の5.67:1）。色相は意味のまとまりで割り当て（暖色=個人/創業家/自社株買い、緑=事業/公益、青=銀行/国内運用、紫〜赤紫=海外運用、赤=アクティビストのみ彩度を上げて別扱い、中立=`var(--ink-tertiary)`）、**4つ目のカテゴリ配色を作らない**。以前はTailwind標準色の場当たり採用で明度がバラバラだった。分類色に薄い面を敷くときは16進アルファの連結ではなく `color-mix()` を使う（中立色に `var()` が入りうるため）。
- **文字サイズのスケール**: 全ページ共通で h1=`text-2xl sm:text-3xl`(24/30px)／h2セクション見出し=`text-xl`(20px)／カード見出し=MUI `h6`(20px)／本文=`text-sm`・MUI `body2`(14px)／メタ・キッカー=`text-xs`・`overline`(12px)。TOPだけ突出して大きい状態（サマリー数値48px・注目カード見出し30px＝h1と同寸）を2026-08-18に是正し、他ページのh2も`text-lg`(18px)から`text-xl`へ揃えた。新しい見出しを足すときはこの表に合わせること。行間・字間はサイズとペアで `globals.css` の `@theme` が持つ（和文はTailwind既定より本文の行間を広く、見出しほど行間比率を下げ字間を締める）ので、`leading-*` / `tracking-*` を個別に足さないこと。バッジ・キッカー用の `text-2xs`(11px) も同所で定義している。
- **一覧の件数とページ送り**: 1ページに100件を超えて描画しない。`/trending`はオートページャー（初回30件＋スクロールで30件ずつ）、`/stocks`・`/investors`は`?page=`のページ送り（`?page=`はクロール可能な素の前後リンク、各ページ自身がcanonical、絞り込みリンクはページ番号を持ち越さない）。カード化で1件あたりの縦幅が増えるため、行リスト時代より件数上限の意味が大きい。
- デザインはエディトリアル（雑誌）系。欧文は`next/font/google`のGeist、**和文は端末内蔵フォント**（iOS: Hiragino Sans / Android: Noto Sans CJK JP）を使う。以前はNoto Sans JPをウェブフォントで読んでいたが、`next/font/google`の`subsets`はプリロード範囲の指定でしかなくCJKの`@font-face`は生成CSSから落ちず、4ウェイト分でunicode-range分割の`@font-face`が496個・378KB（gzip 130KB）のレンダリングブロッキングCSSになっていた。加えて本文の漢字に応じて70〜90KBのwoff2スライスをウェイトごとに追加ダウンロードしていた（スマホで表示が重い直接の原因）。フォントスタックは`src/app/globals.css`の`--font-sans`で定義する。配色はクリーム地の紙面(`--background`/`--paper`)＋インクネイビー＋くすみゴールドのアクセント（`src/app/globals.css` のCSS変数で調整可）。バッジ・カテゴリ表示はピル型からドット＋スモールキャップス文字（`.kicker`）のキッカー表記に変更し、カードは影で持ち上げる代わりに罫線区切り＋タイトル下線ホバーのシンプルな見せ方にした。記事詳細の本文冒頭にはドロップキャップ（先頭一文字の大型表示）を適用。ヒーロー枠（注目記事カード）はアイキャッチ画像がある記事のみ大きな高さを取り、無い記事では余白を残さないコンパクトな表示にフォールバックする。
- 記事一覧（TOP・カテゴリ別一覧・銘柄別履歴）は取引日(`dealDate`)の新しい順、同日内は金額規模(`dealAmount`)の大きい順にソートし（`src/lib/microcms.ts` の `orders: "-dealDate,-dealAmount"`）、`src/lib/groupByDealDate.ts` で取引日ごとに見出しを付けて表示する（見出しは`src/components/DealDateHeading.tsx`で3ページ共通）。「いつの話か」が一覧性で分かるようにするため。各見出しの下には、その取引日の全記事を一覧できる`/date/[date]`アーカイブページへのボタン（`src/components/DealDateSeeMoreLink.tsx`）を表示する。
- ヘッダーのロゴ（🐋アイコン）・カテゴリ別一覧のパンくずリストから常にTOPへ戻れる（記事詳細・銘柄別履歴には既存のパンくずリストあり）。
- フッター（`src/components/Footer.tsx`）は日英両方のlayoutに置き、全ページ共通で表示する。「主要ページ」「一覧・アーカイブ」「サイト情報（このサイトについて・FAQ・お問い合わせ・プライバシーポリシー・利用規約）」「フォロー（公式X・公式YouTube・RSS・言語切替）」の4グループ＋免責の定型文＋著作権表示という構成（モバイル2列・sm以上4列）。一時は「オートスクロールで記事一覧が際限なく伸び、最下部まで到達するのが困難」という理由で廃止しハンバーガーメニューに集約していたが、メニューは開かないと見えず、`/terms`のようにどのページからもリンクの無い固定ページが生まれていたため2026-08-18に復活させた。一覧ページで到達しづらい問題への手当てとして`src/components/HeaderMenu.tsx`（ヘッダー右上のハンバーガーメニュー）は併存させ、スクロール位置によらず主要リンクへ行けるようにしている。
- ヘッダー上部のハブナビ（今週の動き／大口投資家一覧／株式銘柄一覧）はスマホ幅では折り返さず横スクロール1行にし（`.no-scrollbar`、`src/app/globals.css`）ている。カテゴリ絞り込み（13カテゴリ）は以前ヘッダーに常設していたが、全ページ共通で常時表示すると場所を取り本文の文脈からも離れて見えるため`src/components/CategoryFilterDetails.tsx`に切り出し、TOPページの見出し「今日の注目取引」の直下にのみ表示する構成に変更した。
- カテゴリフィルター（`/category/[category]`、`CategoryFilterDetails.tsx`）はmicroCMS側に別フィールドを持たず、`dealType`の値をそのままカテゴリ名として使う（`src/types/article.ts` の `categoryLabel`/`DEAL_TYPE_BY_CATEGORY`、値はidentity）。CMS側の選択肢リストをdealTypeの分類と別途同期させる必要が無く、選択肢の同期漏れによる不具合が起きない構成にしている。
- `/investors`・`/stocks`のカテゴリ／業種フィルターはクライアントJSを使わず、`searchParams`（`?category=`/`?sector=`）を読んでサーバー側で絞り込んだ結果を返すシンプルな構成（フィルターの実体は`<Link>`をボタン化した`src/components/FilterButtonNav.tsx`で、選択中のみ塗りつぶし表示）。`/investors`はカテゴリ別、`/stocks`は業種別で、切り口をあえて分けている。
- ボタンUI（押す導線）は全ページでMUI Buttonに統一している。共通ラッパは`src/components/ActionButton.tsx`（内部リンクは`component={Link}`、外部リンクは`component="a"`＋`target="_blank"`。Server Componentから関数を渡せないため`"use client"`をこのファイルに置いている）で、配色・角丸・フォントは`src/theme.ts`の`MuiButton`（デフォルト`variant="outlined"` / `size="small"`、色は`globals.css`のCSS変数を参照）で一元管理する。適用箇所は取引日見出し下の「この日の記事を見る」（`DealDateSeeMoreLink.tsx`）・記事詳細の共有ボタンと銘柄履歴への導線・月別アーカイブの前後の月ナビ・TOPのカテゴリ絞り込み・一覧ページの絞り込みナビ。本文中の文脈依存のリンク（`/about`・`/faq`の説明文中のリンクなど）はテキストリンクのまま残す。
- `/stocks/[code]`の会社情報（`src/lib/companyInfo.ts`/`src/components/CompanyInfoCard.tsx`）はトレーディングシステム側が日次で更新するSupabaseの`jpx_stock_list`（業種・事業内容の1文説明・株主優待）と`gen_rankings`（直近90営業日分の終値・PER・PBR・52週レンジ位置）を参照する。事業内容はカード内の付加情報ではなく見出し直下に地の文（クローラーが読む本文）として表示し、`generateMetadata`のdescription（`companyInfo.description`＋`formatStockDealSummary()`の取引サマリー）にも組み込むことで、検索結果スニペット・構造化データの両方に銘柄固有の内容が反映されるようにしている（カード内表示のみではSEO評価にほぼ寄与しないため2026-08-15に変更）。株価推移グラフは外部チャートライブラリを使わず、直近90営業日分の終値をインラインSVGの折れ線（`polyline`）で自前描画している。`gen_rankings`の`drop_prob`（下落確率）・`recommend`（売買シグナル）はstock-alert本体の提供価値そのものなので、ブログ側では意図的に表示しない。取得失敗時（未設定・障害）は記事一覧の表示を止めないよう`null`を返しカードごと非表示にする。ページ全体に`export const revalidate = 86400`を設定し、Supabase側のfetchも含めて1日周期で再検証する（データ源が日次更新のため。2026-08-22に5分から延長）。
- ヘッダー右上の検索（`src/components/StockSearch.tsx`）から企業名・証券コード・投資家名で検索できる。PC（md以上）は常時表示の検索窓、モバイルは横幅をロゴに使いたいので🔍アイコンをタップして開く形（2026-08-18）。PCの平常時は素の`<input>`で、フォーカスした時点で`StockSearchPanel`（MUI Autocomplete）を`next/dynamic`で読み込んで差し替えるため、検索窓を常時出しても初期JSは増えない。差し替え中に打った文字を落とさないよう、パネルのマウント通知（`onReady`）が来るまで素のinputを残し、入力値は`StockSearch`側のstate（`inputValue`でAutocompleteに反映）で持つ。マウント直後にMUIが`reason="reset"`で空文字を流すため、`onInputChange`はユーザー操作由来（`input`/`clear`）だけ拾う。入力停止から300ms後に`/api/stocks/search`（`lib/microcms.ts`の`searchStocks()`＝記事の`stockCode`/`stockName`の`[contains]`部分一致、＋`lib/companyInfo.ts`の`searchStockMaster()`＝Supabase `jpx_stock_list`のコード前方一致・社名部分一致。記事側だけだと記事化されていない銘柄が検索から消えるため両方を引く。銘柄単位で重複排除・最大20件）を叩き、結果をクリック（またはEnterで先頭候補）すると`/stocks/[code]`（銘柄別履歴）に遷移する。

## コンテンツの自動生成（任意）

リポジトリルートの `web/publish_blog_articles.py` が、EDINET大量保有報告書（保有比率が増加する取得＝買い方向、減少する譲渡・売却＝売り方向の双方）を基にClaudeで解説記事を生成し、このAPIへ即時投稿する（GitHub Actions `daily_alert.yml` Step 5c、日次）。取得・売却金額(億円)はyfinanceの発行済株式数×株価×保有比率変化からの推定値であることを本文に明記させている。売り方向の記事は`tags`に`"売り"`を追加して買いと区別する（microCMSのセレクトフィールド追加を避け、既存の自由記述`tags`フィールドを流用）。フロント側は`src/lib/format.ts`の`isSellArticle()`が`tags`から判定し、`src/components/DealDirectionBadge.tsx`が分類バッジの隣に「売り」バッジを表示する（買い方向の記事にはバッジを出さない）。

`dealType`（提出者の投資家分類）は、Supabaseの`edinet_filer_classification`マスター（Web検索で確認済みの投資家分類テーブル、バックテスト分析とも共用）をまず参照し、未登録の提出者のみClaudeの一般知識で判定して結果をマスターへ保存する（`web/publish_blog_articles.py`の`classify_filer()`）。キーワード一致だけでは日系/外資の区別やスペース無し個人名を正しく判定できないため。判定不能な場合は「その他」に丸める。

`filerName`（記事詳細の「取引企業」欄）には、EDINET開示から取得した提出者名（`filer_name`）をそのまま送信する。フロント側の`/investors/[filer]`は投資家名をパスパラメータにしているため、両者は常に一致する。ただしmicroCMS側にこのフィールドが未作成のため送信値は現状すべて破棄されている（上記スキーマ表の注意書きを参照）。

投稿後の内容確認・修正はmicroCMS管理画面で人間が行う想定。詳細はスクリプト冒頭のdocstringを参照。

## スコープ外

認証・会員機能、コメント機能。
