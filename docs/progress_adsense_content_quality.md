# AdSense審査「有用性の低いコンテンツ」対応

2026-08-24 に AdSense 審査で不承認（理由: 有用性の低いコンテンツ）。
Google のポリシー3本（コンテンツの最小要件 / 独自性のある質の高いコンテンツ /
ウェブマスター品質ガイドライン: 質の低いコンテンツ）に照らして原因を実測し、改善する。

## 実測した原因（2026-08-24）

| ページ種別 | 件数 | 本文の実態 | 判定 |
|---|---|---|---|
| 記事 `/articles/[id]` | 976 | **中央値455字・100%が1,000字未満・98%が600字未満** | 最小要件に不足 |
| 銘柄 `/stocks/[code]` | 664 | 記事1件だけが482件(73%) | cookie cutter |
| 取引日 `/date/[date]` | 223 | 記事1件だけが87件、2件以下が149件 | thin content |
| 投資家 `/investors/[filer]` | 2,972 | 解説文なしが2,152件(72%)、開示1件のみが1,002件 | cookie cutter |

- サイト全URL 4,756件のうち **75%が自動生成の集約ページ**。
- 記事生成プロンプトは「650〜900字」と指示しているのに実出力は455字で、指示自体も薄い。
- 文の完全一致（コピペ）は無いが、全記事が「①誰が何%取得 ②推定金額と株価 ③※推測」の
  3段落テンプレートで、Google の "cookie cutter" に該当する。

## 対応

- [x] 1. 薄い自動生成ページを noindex にする（コスト0）
  - [x] 取引日: 記事3件未満は noindex（223→index 74件）
  - [x] 投資家: 解説文なし or 開示1件のみは noindex（2,972→index 567件）
  - [x] 銘柄: 記事1件のみ かつ 事業内容の説明なしは noindex（664→index 637件）
  - [x] sitemap を noindex 判定と一致させる（判定は lib/pageIndexability.ts に集約）
- [x] 2. 記事生成プロンプトを厚くする（新規記事）
  - [x] Supabase の既存データから独自の事実を追加投入（`build_context_facts()`）
        保有の積み上げ履歴 / その投資家の他の保有銘柄（業種つき） / その銘柄の他の大株主 /
        開示日時点のPER・PBR・52週レンジ位置・業種。すべて point-in-time。
        ※提出者の過去リターン実績は `filer_win_rate` テーブルが既に廃止されており、
          都度計算が必要なため今回は見送り（効果が要るなら別途）
  - [x] 目標文字数 650〜900字 → 1,300〜1,700字（検証: 455字 → 800〜1,206字）
- [ ] 3. 既存976記事のリライト（**オーナー指示によりAPIバッチではなくClaude Codeが直接執筆**）
  - [x] `tools/export_article_fact_cards.py`: 記事1本ぶんの事実をJSONで書き出す
  - [x] `tools/apply_rewritten_articles.py`: 書いた本文をPATCHで反映（反映前にlogs/へバックアップ、
        既存より短い本文は弾く）
  - [x] `tools/rewrite_thin_blog_articles.py`（APIで書かせる版）は**2026-08-29に削除**。
        「今回は使わないが将来のために残す」としていたが、2026-08-28に夜間385本＋朝225本＝
        610本をこのツールで流し、再生成412回と合わせて本文生成1,022回・推計$11のAPI課金が
        発生した（オーナー指示の「APIバッチではなくClaude Codeが直接執筆」から外れた実行）。
        オーナー判断で経路ごと廃止。共通ヘルパーは `lib/article_text.py` へ移設済み
  - [x] **72件完了**（取引日の新しい順）。実測 352〜697字 → 554〜1,177字
  - 反映済みIDは `logs/rewritten_article_ids.txt` に追記され、事実カードの書き出し側が
    それを読んで候補から外す（中断しても再開できる）
  - [ ] 残り964件（1バッチ8〜12件・1バッチにつき1往復。全件だと80〜120往復かかる）
  - **リライト不能な記事が全体の約20%**（実測107件中22件）。誤報になるため書かずに
    `logs/unwritable_article_ids.txt` へ退避し、最後にまとめてnoindexか削除を判断する:
    - 変化幅0pt＝何がどれだけ動いたのか書けない
    - 保有比率も変化幅も1%未満＝「大量保有報告書」として示せる中身が無い
      （例: 野村證券0.03%、横河電機0.33%で推定898億円という桁の合わない金額）
  - 同一開示で**記事が2本ある重複**を発見（スタートライン477A）。`logs/duplicate_article_ids.txt`
  - **リライト不能な記事は削除した（オーナー判断、2026-08-25）**。計74件。全フィールドを
    `logs/deleted_articles_*.json` へ退避済み（microCMSへ再投稿すれば復元可）
    - 保有比率も変化幅も1%未満で示せる中身が無いもの 58件
    - 変化幅0ptで売買量が不明かつ保有も5%未満のもの 15件
    - 同一開示から二重生成された重複 1件
    - **保有5%以上のものは削除せず救済**（17件）。変化幅を主張しない書き方に直し、
      データと矛盾するタイトルも差し替えた（例: メタウォーター9551は保有19.48%なのに
      タイトルが「17.5%から低下」だった）
  - 提出者を特定できず材料が作れない記事が182件あったが、記事側のfilerNameとタイトルから
    一意化する `resolve_filer()` を入れて**154件を救済**。残り28件はリライト不能
  - 事業内容が**別会社の説明**になっている例を発見（LIFULL 2120＝不動産情報サイトなのに
    「葬儀・終活関連サービス」）。該当記事では事業内容に触れずに書いた

### 追加で見つけて直したもの
- **同一開示の重複記事が旧世代に大量に残っていた**（実例: `/stocks/9706` 日本空港ビルデングに
  同一記事が11件）。EDINETの開示自体は9706で全期間2件しか無く、うち1件は比率変化0ptで
  記事化対象外なので、11件は「同じ開示の作り直し」がそのまま積み上がったもの。原因は
  2026-08-15より前の記事が`filerName`/`ratioChangePct`を持たず、`already_published()`が
  概算金額`dealAmount`±0.05億円でしか突き合わせていなかったこと（開示当日の終値が価格
  キャッシュに入る前後で金額が変わるたびに別イベント扱いになる）。`cleanup_duplicate_blog_articles.py`
  も`filerName`が空の記事をスキップしていたため、この世代は一度も回収されていなかった。
  - 旧記事は**銘柄コード＋開示日＋タイトル**で突き合わせるようにした（旧記事もタイトルに
    提出者名と比率が入る）。`--all`で全期間、`--code`で銘柄を絞れる
  - 日次CIの遡りを3日→30日に広げた（その日の便で取り逃した重複が永久に残るのを防ぐ）
  - 本番の重複記事はオーナーがmicroCMS管理画面で削除済み（2026-08-25）
  - **再発防止**: `already_published()`が(1)照会を開示日まで絞り込むようにし（銘柄コードだけで
    引いて`limit=50`を被せていたため、記事が50件を超える銘柄で既報が応答に入らない穴があった）、
    (2)照会に失敗したら既報扱いで投稿を見送るようにした（従来はFalseを返して投稿していた＝
    判定不能のまま重複が恒久的に残る）。`publish_buyback_articles.py`の同名関数と同じ方針。
    毎時走るので見送っても次の便で取り直せる
- 事業内容(`jpx_stock_list.description`)の64銘柄に、web検索の`<cite index="7-1">`タグが
  そのまま保存され、銘柄ページ・`/trending`・metadataに文字として表示されていた。
  生成側で除去するよう修正し、既存データも一括修正済み（`tools/strip_html_from_descriptions.py`）。
- [x] 4. 反映確認 → AdSense に再審査をリクエスト（2026-08-29 完了）


## 2026-08-25 審査員エージェント3体による再監査で判明したこと

「1回目の対応は十分ではない」というオーナーの見立ては正しかった。**Phase 1（noindex）は
本番に1行も出ていなかった。**

### 本番デプロイが24時間以上まるごと失敗していた（最大の発見）

Vercelの本番デプロイが 2026-08-24 22:24 以降すべて Error。最後に成功したビルドは1日前で、
`77ce0f0f`（noindex対応）以降の変更は一つも本番に反映されていなかった。
進捗ファイルの「[x] 薄い自動生成ページを noindex にする」は**コード上は事実だが本番では偽**
だった。原因は独立した2つのビルドエラー:

1. `src/lib/investors.ts` の `getFilersWithProfile` が `unstable_cache` から `Set` を返していた。
   `unstable_cache` は戻り値をJSONで直列化するため `Set` は `{}` になり `.has()` が消える。
   noindex判定を `generateMetadata` で呼ぶようにしたことで、`/investors/[filer]` の
   プリレンダリングが `TypeError: e.has is not a function` で落ちビルドごと失敗していた。
   → 配列でキャッシュしSetは読み出し側で組み立てる（`companyInfo.ts` の既存の書き方に統一）。
2. `(en)/en/articles/[id]` の `DEAL_TYPE_EN[article.dealType].label` が、dealType未設定の
   自社株買い記事で `undefined.label` になり `generateMetadata` が例外を投げていた。
   → オプショナルチェーンでガード。

**教訓: 「実装した」と「本番に出た」は別。以後 noindex/サイトマップ系の変更は必ず
`curl` で本番の実測値を確認してから完了扱いにする。**

### 自社株買い記事の重複が毎時1本ずつ増え続けていた

microCMSの `dealType` はセレクト型で、**選択肢に無い値をPOSTしてもエラーにならず空配列で
保存される**。「自社株買い」はCMS側に選択肢が無いため全記事で空になっており、これが
3か所で無言の不具合を起こしていた。

- `already_published()` が `dealType[contains]自社株買い` で既報判定 → 常に0件＝「未投稿」
  → 毎時の `edinet_blog.yml` が同じ開示を何度でも新規投稿。コンヴァノ(6574)の同一開示から
  **13本の重複記事**が公開され、トップページと `/stocks/6574` が同一タイトルで埋まっていた。
  Googleの言う scaled content abuse に直撃する状態。
- `cleanup_duplicate_blog_articles.py` は `filerName` 空の記事を対象外にしており、
  発行体自身の開示でfilerNameを持たない自社株買い記事を回収できなかった。
- フロント側も `dealType==="自社株買い"` が常に偽になり、`/buybacks` の記事一覧が0件、
  記事ページの「（上限）」表示が「（概算）」に化けていた。

→ すべて `tags` で突き合わせるよう修正（e45963e3）。重複12件は削除済み
（`logs/deleted_duplicate_buyback_articles_20260825.json` にバックアップ）。

### 修正後の実測（本番）

| 項目 | 対応前 | 対応後 |
|---|---|---|
| sitemap 合計URL | 4,824 | **3,006** |
| 　取引日 | 224 | **90** |
| 　投資家 | 1,978 | **570** |
| 　銘柄 | 1,346 | **1,188** |
| 　記事(ja) | 873 | **794** |
| 薄い日付ページのrobots | `index, follow` | **`noindex, follow`** |
| コンヴァノ重複記事 | 13本 | **1本** |
| `/buybacks` の記事一覧 | 0件 | **3件** |
| `/en/articles/*` の500 | 14件 | **解消** |

### 残っている課題（未対応）

- [ ] **記事本文が薄い**: 933本・中央値461字・90%が600字未満・**94%が見出しゼロ**。
      リライト済みは53本（5%）にとどまる。noindexで隠すのではなく、書くか消すかで決める。
- [ ] **出典不明の「弊社リスクモデル」**: 283本（30%）が「弊社モデルでは下落リスク水準を◯◯と
      評価」と書いているが、モデルの説明ページが無い（`/methodology` は404）。
      YMYLで検証不能な独自指標を根拠に出している状態。methodologyページを作るか記述を消すか。
      出所は `web/publish_blog_articles.py:1110` / `publish_buyback_articles.py:246` が
      プロンプトに渡している `context_dp_level`。
- [ ] **お問い合わせ窓口が無い**: `/contact` `/inquiry` 等すべて404。窓口はXのDMのみで、
      プライバシーポリシーが「お問い合わせフォームがなく」と自認している。
      匿名運営は維持したまま `contact@kujira-watch.com` を出すだけで解消する。
- [ ] **広告枠が本番で0件**: Vercelに `NEXT_PUBLIC_ADSENSE_INFEED_SLOT` /
      `NEXT_PUBLIC_ADSENSE_BOTTOM_SLOT` が未設定で、`AdUnit` が `return null` している。
      `NEXT_PUBLIC_*` はビルド時にインライン化されるため、設定後は再デプロイが必要。
- [ ] **EEA/UK向けCMPが無い**: 英語版を公開している以上、Google認定CMPでの同意取得が要る。
- [ ] プライバシーポリシーに制定日・改定日が無い。
- [ ] `/articles`（記事一覧）が404。873本に対して通覧導線が無い。
- [ ] `not-found.tsx` が無く404がNext.jsの素の画面。
- [ ] TikTok検証ファイル `src/app/tiktokxiQaSwLwWW8L1y11f8rmBurV9SzQtr5C.txt/` が残骸。


## 2026-08-27 記事の数値そのものが誤っていた（薄さより重い問題）

リライトに着手する直前、事実カードの「推定取得金額」が変化幅に対して大きすぎることに気づき、
yfinanceの発行済株式数で検算したところ、**保有総額を「この開示での取得金額」として
公開している記事**が見つかった。全965記事をEDINET開示原本と再照合して是正した。

### 実際にあった誤り

| 記事の記述 | 実際 |
|---|---|
| AGC(5201)「293.7億円で取得」 | 33.2億円（8.8倍） |
| 電通グループ(4324)「189.8億円で取得」 | 11.1億円（17倍） |
| 横浜ゴム(5101)「578.9億円」 | 60.2億円（9.6倍） |
| 日東紡績(3110)「73.8億円分を取得」 | 4.2億円の**売り**（方向も逆） |
| 「Jトラスト、KeyHolderの約30%を取得—41.4億円」 | 保有比率は動いていない |
| 「日本レイが東テクの14.88%を取得、240億円超を投下」 | 保有比率は動いていない |

実在の上場企業・金融機関について、起きていない取引を見出しで断定していた。
薄いコンテンツ以前に誤報であり、YMYLサイトとして最も踏んではいけない部類。

### 原因

`tools/fix_misreported_blog_articles.py` の検出条件が「`ratioChangePct`がEDINETとズレている」
ことを前提にしており、**そのフィールドを保存していなかった時代の記事を素通りさせていた**。
当時の生成側は直前保有割合が取れない開示で「今回比率の全量＝今回動いた分」とみなしており
（この生成側の不具合自体は2026-08-19に修正済み）、その頃の記事が残っていた。

検出条件を2つ追加した:
- (c) 直前保有割合が欠損した開示で、記事が保有比率の全量を変化幅として使った痕跡があるもの。
  過去開示から前回比率を補って判定する（新規の大量保有報告書は全量で正しいので除外）。
- (d) 保有比率が前回と同一（変化幅0pt）なのに推定取得/売却金額を出しているもの。
  担保契約や共同保有者の変更で再提出された変更報告書で、売買は起きていない。

### 結果

- 14本を訂正（タイトル・金額・変化幅・売買方向・本文）
- 売買を伴わない12本を削除（`logs/deleted_misreported_articles_20260827_123840.json`）
- 再照合の結果、**提出者まで一意に特定できた864本に誤報は0件**

### 検証の限界と、集計時の注意

- **100本は開示を一意に特定できない**（提出者名が未保存の旧記事、同一銘柄・同一日に複数の
  提出者がいる開示）。この範囲は誤報の有無を検証できていない。
- 最初の照合では提出者を突き合わせずに「銘柄コード×開示日」だけで開示を引いていたため、
  同じ日に複数の提出者がいる銘柄で無関係な行を拾い、誤報件数を大幅に過大集計した
  （179本・40本という数字を一度出したが、提出者まで一致させると26本だった）。
  **記事と開示の突合は必ず提出者名まで一致させること。**

## メモ

- Anthropic API は 2026-08-24 時点で利用可能（8/23の月次上限は解消済み）。
- 再審査は 1〜4 が全部終わってから。中途半端な状態で出すと再度落ちる。

## 2026-08-29 再審査前チェック（2回目・実測）

本番（kujira-watch.com）とmicroCMS・EDINET(Supabase)を実測して再点検した。
前回（8/25）に挙げた課題はほぼ解消していたが、**送るのを止めるべき問題が1つ残っている**。

### 解消を確認したもの（本番実測）

| 項目 | 実測値 |
|---|---|
| 記事本文の中央値 | **941字**（8/25は461字）。index対象900本では中央値921字 |
| 600字未満 | 全1,127本中45本(3%)。index対象では40本（8/25は90%が600字未満） |
| 見出し(h2)なし | 162本(14%)（8/25は94%） |
| sitemap 合計 | 3,244 URL（pages 56 / stocks 1,306 / dates 95 / investors 574 / 記事ja 900 / 記事en 313） |
| 薄い日付ページ | `/date/2026-08-01` `/date/2025-12-30` とも `noindex` を本番で確認 |
| カニバリ記事 | 重複した`/articles/x7-x7mgxq`は`noindex, follow`、勝ち残り側は`index, follow` |
| プライバシーポリシー | 制定日・最終改定日あり。お問い合わせ窓口（X DM）を明記 |
| `/articles` `/faq` `/terms` `/about` `/privacy` `/en/*` | すべて200 |
| ads.txt | `google.com, pub-8159915946273539, DIRECT, f08c47fec0942fa0` を配信中 |
| AdSenseローダー | 記事ページのHTMLに `adsbygoogle.js?client=ca-pub-8159915946273539` を確認 |
| TikTok検証ファイルの残骸 | 削除済み |
| 記事とEDINETの数値照合 | 8/27以降に作られた233本のうち216本が一致。不一致1本、残り16本はTDnet自社株買い（EDINET側に無いので対象外） |

### 送る前に直すべきもの

- [ ] **タイトルが売却を「新規保有」と書いている記事が11本（うちindex対象10本）**。
      本文は「保有比率を4.12%まで引き下げた」と正しく書いているのに、
      タイトルとtitleEnが「4.12%を新規保有 / Takes 4.12% Stake」になっている。
      タグも「売り」が付いており、記事内で主張が矛盾している。実在の投資家について
      売ったものを買ったと見出しで断定しており、8/27に是正した誤報と同じ部類。
      `ratioChangePct`は正しい負値なので、**タイトルを組み直すだけで直る**（本文の再生成は不要）。
      該当: o383fgmru / yh2xpn243i / fdrlaxeem / gyv-dirm754w / a-kw_7d6j / c_mi8olx40u /
      4moj-7-xu0 / xaabu65o9r / r94-vtvx2t6 / wy8pmt2lz-5 / t597d5b2w
      ※ a-kw_7d6j はタイトルが7.28%、本文が6.21%で数値もずれている。
- [ ] **「弊社モデルによる下落リスク水準は〜」が28本に残っている（うちindex対象23本）**。
      うち21本は「評価対象外でした」で、読者に何も伝えていない。`/methodology`は404のまま。
      `tools/strip_drop_model_mentions.py` は8/25に走らせたが、8/6〜8/21生成分が残っている。
- [ ] **同一開示で数値が食い違う記事1本**: cfff7g1x2f（日本製麻3306）は変化幅-11.6ptだが、
      EDINETの同日開示は(33.52←37.55)と(41.18←49.15)の2件で、どちらとも一致しない
      （別開示の前回比率と今回比率を混ぜている）。index対象。

### 判断が要るもの（送るのを止めるほどではない）

- EEA/UK向けの同意管理（CMP）が未設定。英語版を公開しているので、AdSense管理画面の
  「プライバシーとメッセージ」でGDPRメッセージを作成しておく（コード変更は不要）。
- 広告ユニットのスロットID（`NEXT_PUBLIC_ADSENSE_INFEED_SLOT` / `_BOTTOM_SLOT`）は
  Vercelに未設定のままで、広告枠は本番で0件。審査はローダーとads.txtがあれば足りるので、
  承認後に設定する。
- トップレベルの未定義URL（例 `/zzz-not-exist`）だけNext.js既定の404画面になる。
  セグメント内（`/articles/存在しないID`）は自作の404が出る。
- 同一（銘柄・開示日・提出者）で複数記事があるグループが22組・超過31本。中身は別々の開示で
  重複ではないが、index対象は各グループ1本だけ（`supersededArticleIds`）に絞られている。

### 結論

上の「送る前に直すべきもの」3件（合計40本弱、うちindex対象34本）を直すまでは再審査に出さない。
本文の書き直しは不要で、タイトルの組み直しと該当文の削除だけで済む。

## 2026-08-29 残り109本の消化と、提出者プロフィールの誤情報の是正

### 記事本文（作業3）は完了

- 残っていた109本をすべて書き直した（Claude Codeが直接執筆、Anthropic APIは本文生成に不使用）。
  内訳: 大量保有66本 / 旧フォーマットで提出者を特定できなかった9本 / 自社株買い19本 /
  同一開示の重複4本（下記のとおり未処理）+ 既存11本の再調整。
- 全1,124記事の実測: **中央値980字（8/24の不承認時は441字）、600字未満は29本（同816本）、
  見出しありは990本（同0本）**。1,000字未満は591本だが、うち587本は台帳のリライト済み記事で、
  中央値付近に密集している。
- 本文の品質チェックは `lib/writing_style.find_ai_tells()` を通した。常套句の混入は0件。
  文末単調の検出は337本残るが、これは見出しと本文をタグ除去で連結して数える実装上の癖で、
  生成側も再生成のきっかけにしていない（`lib/writing_style.py` のコメント参照）。

### 実在の個人についての誤った経歴を配信していた（本文の薄さより重い）

事実カードを作る過程で、`edinet_filer_classification.description` に生成AIが書いた経歴が
**個人1,370件ぶん**保存されており、うち45件が実在の別人と取り違えていることが分かった。

| 提出者 | 保存されていた説明 | 実際 |
|---|---|---|
| 加藤　公一レオ | 「日本の政治家・衆議院議員（立憲民主党）」 | 売れるネット広告社の創業者 |
| 仲　暁子 | 「日本の政治家・衆議院議員」 | Wantedlyの創業者 |
| 南部靖之 | 「セコム創業者」 | パソナグループの創業者 |
| 細川　馨 | 「日本の政治家・元首相」 | 別人 |
| 正垣　泰彦 | 「すかいらーくグループの創業者」 | サイゼリヤの創業者 |

この説明文は記事生成のプロンプトに渡っており、**記事3本に「加藤公一レオ衆議院議員（立憲民主党）」
として公開されていた**（うち1本はタイトルと英語版にも記載）。実在の個人についての誤情報であり、
薄いコンテンツ以前の問題。

- 対応1: 個人1,371件の行を `logs/filer_classification_individuals_backup_20260827.json` へ退避し、
  `category='個人'` の `description` を全件クリアした。
- 対応2: `classify_filer()` が個人には説明文を持たせないよう修正（生成側の再発防止）。
- 対応3: 該当記事3本を日本語・英語とも書き直し、タイトルも差し替えた。
- 残課題: `profile`（/investors/[filer] に出る800〜1000字の解説）は個人138件に存在し、
  こちらにも誤りがある（永守重信氏の会社名を「ニッドー」、藤巻米隆氏について別人の名前を挙げて
  迷っている文章など）。**未対応**。

### 金額の誤りが残っていた旧フォーマット記事7本を是正

提出者名を保存していない時代の記事は、8/27の全件再照合で「開示を一意に特定できない」として
検証から外れていた。今回EDINET原本と突き合わせて7本の数字を直した（本文・タイトル・
`dealAmount`・`ratioChangePct`）。

| 記事 | 誤 | 正 |
|---|---|---|
| サンクゼール(2937) | 「9.13%保有分を売却　推定14.8億円」 | 変化幅は**0.05pt**・0.08億円 |
| パンチ工業(6165) | 「3.5億円規模を売却」 | 変化幅は**0.29pt**・0.42億円 |
| データセクション(3905) | 「10.79%を売却　推定25.7億円」 | 変化幅は**4.33pt**・10.3億円 |
| エム・エイチ・グループ(9439) | 「39.4%を保有」 | 8.82%→39.4%の**買い増し**・6.1億円 |

### 未処理: 同一開示の重複4本（オーナー判断待ち）

同じ銘柄・同じ開示日・同じ提出者について、旧フォーマットと現行フォーマットの記事が
二重に存在する。旧側は金額も誤っている（カヤックは同じ売却を13.2億円と0.7億円で二重掲載）。
書き直しても内容が同一になるため、削除を推奨。

| 残す（リライト済み） | 消す候補（旧・重複） |
|---|---|
| poc52w-s3e カヤック 柳澤大輔 0.7億円 | soc_x-n4d 「20.84%保有株を売却 推定13.2億円」 |
| 7b0ecsldoh ジェイHD 眞野定也 1.5億円 | k00d6pvurlz 「29%超保有」推定3.9億円 |
| x3x6h2s0_pa2 ノザワ 玉井章友 8.9億円 | 8t0_9gi57 「6.21%を取得、9.0億円」 |
| 8_bijyurt 理研ビタミン 保坂晴彦 101.2億円 | c_q5_eecptbo 「11%超を取得、102.4億円投資」 |

### 2026-08-29 是正を実行した（オーナー指示「全て実行して」）

microCMSへ反映済み。全件、変更前の全フィールドを `logs/adsense_fix_backup_20260829_*.json` へ
退避してある（再投稿すれば復元可）。

| 対応 | 件数 | 内容 |
|---|---|---|
| タイトルのみ差し替え | 4本 | 本文はEDINETと一致していたので `◯%を新規保有` → `保有比率◯%に引き下げ` に。英題も `Takes X% Stake` → `Cuts Stake … to X%` |
| タイトル＋変化幅＋金額＋本文 | 4本 | 変化幅と推定金額を開示から計算し直し、本文の該当文を書き換え |
| 記事を削除 | 3本 | 同日に4〜6件の開示があり事実を一意に再構成できないもの（o383fgmru / r94-vtvx2t6 / t597d5b2w） |
| 「弊社モデルによる下落リスク水準は〜」の一文を削除 | 28本 | 文を消すだけ。削除後に空になる段落は無かった |
| 英語本文の書き直し | 8本 | 「400日以内に開示が無い＝新規取得」という誤った前提で売却をnew acquisitionと書いていた。ビットコイン・ジャパンを暗号資産企業、ベクトルを動画クリエイター事業と説明する誤りも同時に修正 |
| 和文と英語版で別々の開示を指していた記事 | 2本 | 7vsta24bo0q6（グリーンモンスター157A）と gg_2tlg5fl（Ridge−i 5572）。和文は変化幅ほぼ0の訂正報告書、英語版と金額はその日の大幅な引き下げ、という食い違い。実体（大幅な引き下げ）に統一 |
| 変化幅が同日の別開示との差分になっていた記事 | 1本 | gn87lk8wjy-1（スターシーズ3083）: -1.59pt→-19.73pt、推定金額 1億円→12.6億円 |

数値を直した記事の推定金額は、生成側と同じ式（変化幅 × 発行済株式数 × 開示日終値）で計算し直した。

**是正後の実測（microCMS 1,124本・全件スキャン）**

| チェック | 結果 |
|---|---|
| タイトル『新規保有』×変化幅マイナス | 0件 |
| 英題『Takes …% Stake』×売り / 『Cuts Stake』×買い | 0件 / 0件 |
| タイトル『引き上げ』×売り / 『引き下げ』×買い | 0件 / 0件 |
| 『弊社モデル』『下落リスク水準』の残存 | 0件 |
| 訂正報告書タイトル×変化幅1pt以上（37本） | 全件EDINET開示と一致（訂正報告書自体が動きを伴う正常なケース） |
| 本文の中央値 | 986字（是正前 941字） |

**未了**
- 本番反映: 記事ページは `revalidate = 86400` なので最大1日、または次の本番デプロイで反映される。
  この時点ではmainに未pushの他作業（英語版除却PR）があるため、こちらからのデプロイは行っていない。
- EEA/UK向けCMP: AdSense管理画面「プライバシーとメッセージ」でGDPRメッセージを作る作業で、
  コード変更ではないためオーナー側の操作が要る。

### 2026-08-29 本番反映を確認した

| 確認項目 | 本番の実測 |
|---|---|
| 修正したタイトル | `/articles/fdrlaxeem` が「保有比率4.12%に引き下げ」で配信されている |
| 削除した3本 | `o383fgmru` / `r94-vtvx2t6` / `t597d5b2w` すべて404 |
| 「弊社モデル」の記述 | `/articles/8r92i2q_m1` の本文から消えている（0件） |
| sitemap 記事(ja) | 900 → **896** URL |

**PR #289（英語版除却）はマージしていない。** マージ規律どおり最新mainを取り込んで
（コンフリクトはREADMEの3行のみ・解消可能）テストを回したところ、ブランチ側で3本が失敗した:

- `tests/test_backfill_blog_eyecatch.py`
- `tests/test_delete_low_value_blog_articles.py`
- `tests/test_publish_blog_articles.py`（`get_filer_profile` がupsertを呼ばない）

同じ3本は `origin/main` では通る。ブランチ最新の `20d9dba7`（お問い合わせページ追加・個人の
プロフィール生成停止）時点で既に失敗しており、こちらのマージ作業が原因ではない。
テストが対象の変更に追いついていないため、作業中のセッションが直してからマージする。
今回の記事の是正は microCMS 側の変更なので、このPRを待たずに本番へ出ている。

### 2026-08-29 再審査をリクエストした

CMP（GDPRメッセージ）をAdSense管理画面で作成・公開し、再審査をリクエストした。設定内容:

- サイト `kujira-watch.com`、デフォルト言語 日本語、別の言語なし（英語版は廃止予定のため）
- ロゴは `kujira-watch.com/logo`（512×512 PNG）を登録。**ロゴ未登録だと公開ボタンが押せない**
- ユーザーの選択: 同意=オン / オプションを管理=オン / **同意しない=表示**（必須項目。空だと公開できない）/ 閉じる（同意しない）=オフ
- 同意メッセージを最適化する=オン

結果はGoogleからメールで届く（通常1〜2週間）。**審査中は記事の大量削除・大量リライト・URL構成の変更を行わない**
（審査対象のURLが動くと不承認の材料になる）。

### 2026-08-29 申請後の全体再チェック（3回目）

「これで通るか」を確かめるため、本番・microCMS・EDINETを横断してもう一度全件見た。
**追加で7本の矛盾が見つかったので直した**（申請後だが、記事の中身の訂正なのでURL構成は動かない）。

#### 追加で直したもの

売却の記事なのに本文が「直近400日以内に開示がない＝実質的な新規保有」と書いていたもの7本。
生成側が使っていた400日ヒューリスティックの名残りで、見出しごと「新規参入で大規模ポジション形成」
となっているものもあった。EDINETの前回保有割合に置き換えた（例: ＮＥ441A ＡＯＩ株式会社は
47.54%→32.19%、日本ファルコム3723 加藤翔氏は40.47%→10.41%）。
さらに引き下げの記事で「今回が実質的な新規の届出にあたります」と書いていた2本も事実だけの記述に直した。
バックアップは `logs/adsense_fix_backup_20260829_*_400days.json` / `*_newfiling.json`。

#### 全件スキャンの結果（1,120本）

| チェック | 結果 |
|---|---|
| タイトルと変化幅の矛盾（新規保有/引き上げ/引き下げ×符号） | 0件 |
| 『弊社モデル』『下落リスク水準』 | 0件 |
| 投資助言に読める表現（買い時・狙い目・推奨・買うべき等） | 0件 |
| 断定的な将来予測（確実に上昇/必ず利益 等） | 0件 |
| HTML・引用タグの残骸 | 0件 |
| 本文が完全一致する記事 | 0組 |
| 訂正報告書タイトル×変化幅1pt以上（37本） | 全件EDINET開示と一致（正常） |
| 和文本文の「新規保有」×売り（4本） | いずれも「同じ日に別の投資家が新規保有を届け出た」で正しい |
| 個人の経歴への言及（4本） | 加藤公一レオ氏=創業者、清原達郎氏・五味大輔氏=著名個人投資家。いずれも公知で正確 |

#### 本番の状態（実測）

- 内部リンク19本すべて200（`/contact` も公開済み）。リンク切れなし
- 英語版は除却され `/en/*` は日本語ページへ308リダイレクト。sitemapの articles-en は0件
- sitemap: pages 39 / stocks 653 / dates 94 / investors 574 / 記事(ja) 892
- `ads.txt`・AdSenseローダー・プライバシーポリシー（AdSense/Cookie/オプトアウトの記載）すべて配信中
- アイキャッチはPexels（商用可・帰属不要）で、画像内に撮影者クレジットを焼き込み済み。
  個人名が出る「個人」分類は人物写真ではなく街並みを使う設計で、人物の取り違えを避けている

#### 残っている弱点（不承認だったらここ）

- **記事の型が揃いすぎている**: 294本が「直近400日以内に同一提出者の開示が確認できないため、
  実質的な新規保有または大幅な保有再開とみられます」という同じ一文を持つ。事実としては
  ヘッジされた書き方で誤りではないが、Googleの言う scaled content の見え方に近い。
  次に落ちたらここを削るか、開示履歴から実際の初回開示日を出す書き方に変える。
- `/articles` は無限スクロールでページネーションのリンクが無く、クローラーはsitemap頼り。
- トップレベルの未定義URLだけNext.js既定の404画面（セグメント内は自作404）。

### 2026-08-29 薄い集約ページをnoindexから404へ（PR #290）

オーナー判断で、noindexにしていた集約ページを「そもそも公開しない」ことにした。
noindexは評価対象から外す指定でしかなく、URLは存在し続けるため。

| 種類 | 存在しうる数 | 公開（sitemap） | 404にする |
|---|---|---|---|
| 銘柄 `/stocks/[code]` | 3,008 | 653 | **2,355** |
| 投資家 `/investors/[filer]` | 2,991 | 574 | **2,417** |
| 取引日 `/date/[date]` | 291 | 94 | **197** |

判定条件（`lib/pageIndexability.ts`）は変えていない。変えたのは「条件を満たさないときに
noindexにするか404にするか」だけ。記事は対象外（noindexの226本は本文中央値1,052字あり薄くない）。

**404にする以上、内部リンクも同じ判定で出し分けが要る**（リンクだけ残すと記事・一覧・ランキング
から大量のリンク切れが出る）。公開集合とhref解決を `src/lib/publishedPages.ts` に集約し、
記事のパンくず・ファクトボックス・本文中の投資家名リンク・ArticleNextStep・銘柄/投資家一覧・
トレンド・アクティビスト・自社株買い・月別・ランキング・TOP・検索API・RSS・構造化データの
すべてで使う。公開していない対象はリンクにせず素のテキストで出す。

検証: tsc / eslint / next build 通過。ローカルの本番ビルドを600URL巡回して**リンク切れ0件**、
非公開の銘柄・取引日とそのRSSが404、公開対象は200を確認。

**マージは審査結果が出てから**。約4,969URLが一斉に404になる変更を審査中に入れると、
クロール中の審査員が大量の404を踏む。
