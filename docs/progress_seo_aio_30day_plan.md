# kujira-watch SEO/AIO 30日改善計画（外部アドバイス第3弾の反映）

最終更新: 2026-08-15（P0〜P4すべて完了。以降は効果測定フェーズ — GSCの前後比較を数日〜数週間後に実施）
対象: `kujira-watch/`（https://kujira-watch.com/ ）+ `web/publish_blog_articles.py` + `web/x_client.py`

## 0. 前提と位置付け

2026-08-15に受けた外部SEO/AIOアドバイス（課題10項目＋30日プラン）を、現状のコードベースと
突き合わせて「実装済み」「本当のギャップ」に切り分けた実行計画。
過去の同種計画（`progress_blog_seo_aio.md` / `progress_discover_seo_plan.md` /
`progress_seo_indexing.md` / `progress_stocks_thin_content.md`）と重複する作業はやらない。

**結論**: アドバイスの🔴S課題①②③⑥（個別ページ・銘柄ページ・投資家ページ・内部リンク）は
**大部分が実装済み**。本当のギャップは「④タイトルの検索クエリ設計」「⑤記事内の構造化された
答え（ファクトボックス）」「⑦ランキングの拡充」「⑨SNS日次サマリー」「⑩KPI計測(GA4)」の5点。
30日はこの5点に集中する。

## 1. アドバイス10課題 × 現状実装の突き合わせ

| # | アドバイスの課題 | 現状 | 判定 |
|---|---|---|---|
| ① | 検索意図別の個別ページが弱い | `/stocks/[code]`(2,978件)・`/investors/[filer]`・`/date/[date]`・`/category/[category]`・`/monthly`・`/weekly`・`/trending`が稼働済み。1開示→記事+銘柄+投資家+日次の複数入口は既に成立 | ✅ 済 |
| ② | 銘柄ページのSEO資産化 | 会社情報カード(`jpx_stock_list`)・銘柄別要約文(`stockSummary.ts`)・大株主(提出者)一覧(`getFilersByStockCode`)・開示履歴・EDINETリンク実装済み。description backfill済(1,310件) | ✅ 概ね済（保有比率の時系列推移のみ弱い→P3） |
| ③ | 投資家ページのSEO資産化 | `/investors/[filer]`に保有銘柄テーブル(`getFilerHoldings`)・投資家分類・報告書種別を表示済み | ✅ 概ね済（比率推移・買い/売り別集計→P3） |
| ④ | タイトル/H1が検索クエリ設計でない | 記事タイトルはClaude自由生成（40字以内の制約のみ）。「銘柄名（コード）」「大量保有報告書」等の検索語を必ず含む保証がない。冒頭1文も検索クエリへの直答になっていない | 🔴 **ギャップ（P1）** |
| ⑤ | 「結局どういう意味？」の解説不足 | ※推測文は本文末尾にあるが、AIが引用しやすい構造化ブロック（保有比率/前回比/推定額/方向/属性の箇条書き）が無い。データは全てmicroCMSフィールドに存在済み（dealAmount, dealType, attentionScore, attentionReasons等） | 🔴 **ギャップ（P1）** |
| ⑥ | 内部リンク構造が弱い | 記事↔銘柄↔投資家↔日付↔カテゴリの網状リンク・パンくず・関連記事（同銘柄→同分類）実装済み（`articles/[id]/page.tsx`） | ✅ 済 |
| ⑦ | ランキング不足 | `/ranking`（投資家リターン）と`/trending`のみ。買い増し/売却/件数/アクティビスト注目のランキングが無い。データは`edinet_large_holdings`から集計可能 | 🟠 **ギャップ（P2）** |
| ⑧ | E-E-A-T/情報源の明示 | `/about`（運営者・データソース・免責）・`llms.txt`・記事の出典URL(EDINET)・`/faq`実装済み。記事内の「提出日・データ更新日時」の定型表示のみ未統一 | ✅ 概ね済（出典ブロック統一→P1に同梱） |
| ⑨ | SNS/外部流入の構造 | 記事単位のX自動投稿（`x_client.py`、画像付き）はあるが、アドバイスの言う「本日のクジラ」日次サマリー投稿（件数・総額・最大買い/売り）が無い | 🟠 **ギャップ（P4）** |
| ⑩ | KPI設計 | Vercel Analytics + Speed Insights + GA4（`G-0Z3FMTXC5B`、2026-08-15導入済み確認）。ただしKPI定義とベースライン記録・定点観測の運用が未定義 | 🟠 **ギャップ（P0）** |

## 2. 30日フェーズ計画

### P0: 計測基盤（Day 1〜3・効果測定の前提）
- [x] GA4導入 → **導入済みと確認**（2026-08-15）。`@next/third-parties`の`GoogleAnalytics`が
      `(ja)/layout.tsx`・`(en)/en/layout.tsx`の両方に`gaId="G-0Z3FMTXC5B"`で配線済み
      （並行セッションのモバイル高速化PRに同梱されmainマージ済み）。追加実装不要。
- [x] KPI定義を固定（本計画の効果判定に使う）:
  - GSC: クリック数 / 表示回数 / CTR / 平均掲載順位（週次で記録）
  - ページ種別PV: 記事 / `/stocks/*` / `/investors/*` / ランキング（GA4のページパス別）
  - 回遊: ページ/セッション
- [x] ベースライン値の記録 — 2026-08-15完了（ユーザーがGSCスクリーンショットを共有）。
      GA4は導入直後でデータ蓄積が浅いため、当面はGSC+Vercel Analyticsを主、GA4を従とする。

  **ベースライン（施策適用前・2026-08-15時点）**
  | 指標 | 値 | 出典 |
  |---|---|---|
  | sitemap総URL数 | 1,713（2026-08-14実測） | progress_seo_indexing.md |
  | 記事数 | 359（2026-08-14実測） | 同上 |
  | /stocks/[code] ページ数 | 2,978 | progress_stocks_thin_content.md |
  | 投資家ページ数 | 2,938（2026-08-15時点） | investors.tsコメント |
  | GSC 合計クリック数 | 45（3か月表示だが実質2026/08/01〜08/13の集計。8/8頃から立ち上がり、8/13は日次12クリック） | GSC（2026-08-15ユーザー共有） |
  | GSC 合計表示回数 | 756（同上。8/13は日次約140表示） | 同上 |
  | GSC 平均CTR | 6% | 同上 |
  | GSC 平均掲載順位 | 9.1 | 同上 |
  | GSC インデックス未登録 | 検出149件+クロール済み104件（2026-08-12） | progress_seo_indexing.md |

### P1: 記事テンプレートの検索クエリ化＋ファクトボックス（Day 4〜10）
対象: `web/publish_blog_articles.py`（生成側）+ `kujira-watch/src/app/(ja)/articles/[id]/page.tsx`（表示側）

- [x] **タイトルのテンプレート化** — 2026-08-15実装（`build_article_titles()`。60字超過時は
      提出者名を…短縮、新規保有は「X%を新規保有」、titleEnはClaudeが同時生成するローマ字名で
      テンプレ組み立て）。
- [x] **冒頭アンサー文の強制** — 2026-08-15実装（プロンプトに1文目を固定指示、英語版も同様）。
- [x] **ファクトボックス** — 2026-08-15実装。記事詳細のdlに「保有比率（前回%併記）」「前回比
      （±pt、買い緑/売り赤）」を追加。保有比率は`getHoldingSnapshot()`でEDINET開示から取得し、
      前回比はCMSの`ratioChangePct`優先・旧記事はEDINET差分で補完（LLM不使用）。
- [x] **出典ブロックの統一** — 2026-08-15実装（情報源=EDINET・提出日・記事公開日・
      元の開示リンク・免責の定型ブロックを本文下に表示）。
- [x] microCMSスキーマ追加（ユーザー作業） — 2026-08-15完了（`ratioChangePct`・`filerName`）。
- [x] テスト更新（68件、+5件）+ README更新 — 同一コミットで対応。
- [ ] 効果測定: 適用後の新規記事のGSC掲載順位/CTRを、適用前1週間の記事と比較（数日〜数週間後）。

### P2: ランキング拡充（Day 11〜17）
対象: `kujira-watch/src/app/(ja)/ranking/` 配下に追加（既存`/ranking`は投資家リターンとして維持）

- [x] `/ranking/buys`・`/ranking/sells`・`/ranking/filings`・`/ranking/activist` — 2026-08-15実装。
      データ源は当初案のSupabase集計ビューではなく**microCMS記事（`getRecentArticles(30)`）**に変更
      （推定金額はPython側でしか算出できず記事に既に入っているため。新規ビュー不要で各行から
      解説記事へも直接リンクできる）。`generateStaticParams`+`revalidate=3600`。
- [x] `ItemList`/`BreadcrumbList`のJSON-LD・パンくず・sitemap登録 — 実装済み。
- [x] `/ranking`本体と記事詳細ページ（「関連ランキング」nav）から内部リンク — 実装済み。
- [x] README更新 — 同一コミットで対応。

### P3: 銘柄・投資家ページの時系列強化（Day 18〜24）
- [x] `/stocks/[code]`: 「保有比率の推移」テーブル（`getHoldingsByStockCode()`、開示日降順100件、
      `RatioTransition`で前回%→今回%＋▲/▼増減付き）— 2026-08-15実装。専用サブページは作らない。
- [x] `/investors/[filer]`: 「直近で買い増した銘柄／売却した銘柄」2カラム＋取引テーブルの
      増減方向表示 — 2026-08-15実装。
- [ ] 両ページのmeta description・要約文への推移反映 — 見送り（既存の`stockSummary.ts`要約が
      買い/売り件数・金額を既に含んでおり、追加の差分が小さいため優先度を下げた）。

### P4: SNS日次サマリー「本日のクジラ」（Day 25〜30）
対象: `web/x_client.py` + `.github/workflows/edinet_blog.yml`（毎時バッチ済みの投稿フローに追加）

- [x] 日次サマリー投稿関数（`post_daily_summary()`）— 2026-08-15実装。件数・合計金額・
      最大買い増し・最大売却を定型フォーマットで1ポスト、`/date/{date}`へのUTMリンク付き、
      データはmicroCMS記事から集計（新規LLM呼び出し無し）。
- [x] 投稿タイミング: 21時JST（12時UTC）の毎時バッチ最終便のみ。時刻ガードが外部ストレージ
      無しの1日1回重複ガードを兼ねる（0件の日は投稿しない）。
- [ ] 週次「今週アクティビストが買った銘柄」— 日次サマリーの効果を見てから判断（初期スコープ外）。
- [x] テスト6件追加 + README更新 — 同一コミットで対応。

## 3. やらないこと（明示）

- **SEO記事の量産・別途特集記事の執筆**: アドバイス自身も「DB型SEOが向いている」と結論。
  既存のデータパイプライン（1開示→記事+銘柄+投資家+日次+ランキング更新）を強化する方向に集中。
- **銘柄ページの期間別サブページ（/stocks/8151/30d等）**: 薄いコンテンツの量産になり、
  現在進行中のインデックス未登録問題（`progress_seo_indexing.md`）と逆行するため作らない。
- **既存記事104件の一括リライト**: 2026-08-14にユーザー判断で放置と決定済み。P1のテンプレは
  新規記事のみに適用（`tools/rewrite_thin_blog_articles.py`は残置済み、必要時に`--ids`で再開）。
- **カテゴリ→投資家個人ページの新設**: `/investors/[filer]`が既に存在するため不要。

## 4. 効果判定の規律

- 各フェーズのマージ前に `npx tsc --noEmit` / `npm run lint` / `python3 tests/test_*.py` で
  デグレ確認（CLAUDE.md §8）。
- SEO施策はバックテスト不能のため、**GSCクリック数・表示回数・対象ページ種別PVの前後比較**を
  マージ後の効果判定KPIとする。インデックス反映は数日〜数週間かかる前提で急かさない
  （[[feedback_seo_indexing_wait]]の運用に従う）。
- 各ステップ完了ごとに本ファイルの `[ ]` を `[x]` に更新してコミットする（CLAUDE.md §6）。

## 5. ユーザーに依頼する作業（ブロッカー）

1. ~~GA4測定IDの発行・共有~~ → 導入済み確認（`G-0Z3FMTXC5B`）。不要になった。
2. ~~GSC数値の共有~~ → 2026-08-15共有済み（ベースライン表に記録）。ユーザー依頼のブロッカーは全て解消。
3. ~~microCMSスキーマに`ratioChangePct`と`filerName`を追加~~ → 2026-08-15ユーザー対応済み。
4. X APIの投稿上限プラン確認（日次サマリー1件/日の追加。無料プランでも1日最大4件で
   月間上限500ポストに収まる想定だが、念のため確認推奨）
