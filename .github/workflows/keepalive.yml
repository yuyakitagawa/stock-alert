name: Keepalive

on:
  schedule:
    - cron: '0 6 * * 1'  # 毎週月曜 06:00 UTC（daily_alertの1時間前）
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: gautamkrishnar/keepalive-workflow@v2
        with:
          use_api: true
